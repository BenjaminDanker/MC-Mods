# Sky-Islands

Simple Fabric mod for Minecraft 1.21.10 that removes all natural creeper spawns across every biome.

## Build

```powershell
# From this folder
.\gradlew clean build
```

Place the resulting jar from `build\libs` into your server/client `mods` folder.
