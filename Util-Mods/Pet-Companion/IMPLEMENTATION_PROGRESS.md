# AI Pet System Implementation Progress

Binding specification: `minecraft_ai_pet_system_implementation_handoff.md`.

An item remains unchecked until it is implemented and verified. Completed items receive a compact evidence note containing changed files/modules, the exact verification performed and result, and any remaining limitations. Unresolved work is tracked under **Open Issues / Decisions Needed**.

## 20. Repository and module checklist

- [x] Locate all existing repositories and working trees. — Evidence: changed `IMPLEMENTATION_NOTES.md`; `git rev-parse/status` plus `rg --files` inspected all supplied MC-Mods, MCServerPortals, Velocity-Wakeup-Lobby, and MPDS roots; result: target is greenfield/untracked and related roots were clean; limitation: website/live-host trees are not available.
- [x] Read any `AGENTS.md`, README, build instructions, and deployment scripts. — Evidence: changed `IMPLEMENTATION_NOTES.md`; searched all supplied roots for guidance/README/build/deploy/systemd files and read applicable `.github/copilot-instructions.md` plus relevant builds/READMEs; result: no `AGENTS.md` or deploy/systemd scripts found; limitation: live-host deployment files remain unavailable.
- [x] Record Minecraft version, Fabric Loader/API version, Yarn/Mojmap mappings, Velocity API version, Java version, and Gradle version. — Evidence: changed `IMPLEMENTATION_NOTES.md`; compared checked-in Gradle properties/builds; result: local Fabric baseline recorded as MC 1.21.10/Yarn build.1/Loader 0.17.3/Fabric API 0.138/Loom 1.11.8/Gradle 8.14/Java 21 and Velocity build as API 3.2.0-SNAPSHOT/Java 17; limitation: deployed versions and conflicting README/build claims remain unverified.
- [x] Locate existing villager text UI implementation. — Evidence: changed `IMPLEMENTATION_NOTES.md`; traced `VillagerEntityMixin`, both chat mixins, and `ConversationManager`; result: confirmed the intended private chat-bar capture/public-broadcast suppression plus the separate automatic villager greeting; limitation: no dedicated client screen exists or is required for the clarified pet input.
- [x] Locate existing portal/shared-payload modules and channel registry. — Evidence: changed `IMPLEMENTATION_NOTES.md`; traced `MC-Portal-Protocol` codec/registration and Velocity channel constants; result: canonical owner and `wakeuplobby:portal_request` confirmed; limitation: future pet transport messages are not designed.
- [x] Locate current Velocity StickyRouter/StickySession transfer code. — Evidence: changed `IMPLEMENTATION_NOTES.md`; traced `PortalCommandHandler`, `StickyRouter`, `StickySessionManager`, and `VelocityPlugin` event/connect paths with `rg` and source reads; result: holding-lobby/final-destination lifecycle mapped; limitation: no pet pre-transfer handshake exists.
- [ ] Locate current website/Stripe integration.
- [x] Locate current database configuration and migrations. — Evidence: changed `IMPLEMENTATION_NOTES.md`; inspected MPDS config/JDBC/DDL code without reading secret values; result: MySQL-protocol config and ad-hoc startup DDL found, with no migration framework; limitation: live MariaDB schema/version/backups/limits are unverified.
- [x] Check for uncommitted user changes; preserve unrelated work. — Evidence: no source files changed for this check; `git status --short` in all supplied roots; result: related roots clean and the entire user-provided Pet-Companion folder was already untracked; limitation: none within supplied roots.
- [ ] Identify how mods are built/copied to all six servers.
- [ ] Identify staging/test server capability.

## 21. Granular implementation checklist

### Phase A — Discovery and design verification

- [ ] Confirm exact server/mod versions and mappings.
- [ ] Confirm whether `generic.scale`/mapped scale attribute is available and test it on cat/wolf hitboxes.
- [x] Enumerate cat and wolf variant registry APIs for the target version. — Evidence: no source change; Gradle-resolved Yarn build.1 jar inspected with `jar tf`/`javap`; result: `RegistryKeys.CAT_VARIANT`/`WOLF_VARIANT`, private entity setters, 11 vanilla cat IDs, and 9 vanilla wolf IDs confirmed; limitation: runtime resource-pack/datapack additions and visual results remain untested.
- [ ] Verify exact Text Display APIs and fixed-orientation yaw convention. — Partial: `PetSpeechDisplayManager` compiled against 1.21.10 and the focused GameTest asserted `FIXED`, pet yaw +180°, scaled height, follow, and expiry; actual client-side visual confirmation of which surface faces pet-forward remains required.
- [x] Verify existing villager UI client/server message flow. — Evidence: changed `IMPLEMENTATION_NOTES.md`; line-by-line trace of signed chat capture, same-tick broadcast suppression, and async callbacks; result: the intended private input transport was confirmed and adapted in Phase F; limitation: deployed coexistence with the Villager-Interface mod remains untested.
- [x] Inventory every backend-switch mechanism. — Evidence: changed `IMPLEMENTATION_NOTES.md`; exhaustive source search across proxy and backend request producers; result: initial join, kick redirect, signed/legacy portal, `/server`, `/connect`, `/forceserver`, `/return`, admission release, tier fallback, and seven backend producers mapped; limitation: deployed third-party plugins cannot be ruled out locally.
- [x] Decide which existing shared protocol module owns new pet payloads. — Evidence: changed `IMPLEMENTATION_NOTES.md`; verified canonical module manifest/codec registration; result: `MC-Portal-Protocol` is the sole owner for any later `wakeuplobby:pet_*` payloads; limitation: authenticated HTTP may eliminate the need for new payloads.
- [ ] Confirm the Raspberry Pi MariaDB `minecraft` schema, current credentials mechanism, schema tool, backup mechanism, and connection limits.
- [ ] Confirm the pet service's chosen host can securely reach that MariaDB instance without exposing it publicly.
- [ ] Confirm existing Stripe Checkout/webhook/customer portal code.
- [x] Write an implementation note containing discoveries and deviations before large edits. — Evidence: added `IMPLEMENTATION_NOTES.md`; manually reviewed against handoff §§1, 6, 20, 24, and 25; result: ownership map, ordering, incompatibilities, and unresolved facts recorded; limitation: it must be revised as deployment facts become available.

### Phase B — Central persistence and state machine

- [x] Add migrations for pets, traits, mood, sleep, events, long-term memory, audits, subscriptions, webhooks, recalls, usage, jobs, and account linking as applicable. — Evidence: added versioned `db/migrations/V001__create_ai_pet_schema.sql`, V002 recall replay fields, and V003 Stripe Checkout binding fields; disposable MariaDB 11.4.10 applied all three in order and the JDBC acceptance test counted all 16 required tables; result: the complete forward schema exists without startup mutation; limitation: production Raspberry Pi database selection/application remains an operator step.
- [x] Add schema constraints and indexes. — Evidence: the same V001–V003 migrations define UUID/scale/state/trait/mood/job/link constraints, foreign keys, unique owner/entity/idempotency keys, and due/location indexes; the real MariaDB acceptance test rejected an invalid HELD placement-field combination and verified the 16-table migrated schema; result: MariaDB enforced the tested state constraint; limitation: full production backup/restore and concurrency drill remain.
- [x] Implement pet repository/service interfaces. — Evidence: added `pet-service` repository/adoption/placement/subscription boundaries, thread-safe `InMemoryPetRepository`, and transactional `JdbcPetRepository`; user clean build ran 10 service tests with zero failures, and `.\gradlew.bat :pet-service:compileJava --offline` compiled the JDBC adapter; result: service boundaries plus development and V001-compatible JDBC implementations exist; limitation: the JDBC adapter still needs a live MariaDB integration test, pooled driver wiring, and HTTP boundary.
- [x] Implement one-pet-per-owner transactional adoption. — Evidence: changed service adoption/repository, authenticated adoption HTTP wire/client, and Fabric command; the in-memory concurrent test passed 1/1 and the new real MariaDB JDBC acceptance test ran eight concurrent adoption requests, yielding one shared pet ID and one persisted owner row; result: unique-index races converge in both repository implementations; limitation: repeat on the production MariaDB version/backup environment.
- [x] Implement bounded appearance randomization and exact persistence. — Evidence: changed adoption randomizer/catalog, persisted aggregate response, and explicit `PET_ALLOWED_SPECIES` parsing/runtime enforcement; `\.\gradlew.bat :pet-fabric:test --tests com.silver.aipets.fabric.authority.HttpPetAuthorityGatewayTest.adoptionTransportEnforcesAccessAndReturnsExistingWithoutReroll --offline` passed exactly 1/1 for disabled-dog rejection with no row, in-bounds cat creation, and a dog/name retry returning the identical original aggregate; focused service/config XML reports each allowlist test 1/1 with zero failures/errors; limitation: live MariaDB round-trip remains and adding a future species requires an intentional domain/wire/catalog change.
- [x] Implement `HELD <-> PLACED` and `PLACED -> TRANSFERRING -> PLACED/HELD` CAS transitions. — Evidence: changed `pet-common` `PetTransitions` and `pet-service` `PetPlacementService`/repository CAS boundary; user-reported clean build ran all 8 transition and 3 placement-service tests with zero failures, including exact place/pickup/compensation, reserve-before-removal, one-shot claim, expiry-to-held, and two concurrent placement requests with one winner; result: pure state changes and atomic repository contract are enforced; limitation: production MariaDB adapter/idempotency storage and proxy/backend transfer wiring remain.
- [x] Implement entity UUID/revision validation. — Evidence: changed `pet-common` `EntityAuthority`/decision types; `.\gradlew.bat :pet-common:test --tests com.silver.aipets.common.authority.EntityAuthorityTest ... --offline` ran 2 tests with zero failures and covered exact identity plus older/equal/ahead revisions; result: an older proven entity can refresh while an impossible ahead revision is rejected; limitation: production service responses still require authenticated transport.
- [x] Implement stale entity decisions. — Evidence: added pure `EntityAuthority` reuse/discard/materialize decisions; `EntityAuthorityTest` verified matching, held, transferring, wrong-backend, wrong-UUID, and missing-entity cases with zero failures; result: deterministic stale decisions feed entity-load and bounded periodic callers; limitation: live remote-recall/offline-backend deployment evidence remains.
- [x] Implement idempotency middleware/storage. — Evidence: production `idempotency_requests` schema/JDBC adapter atomically reserves `(scope,key)`, verifies canonical fingerprints, leases in-progress work, and stores exact responses; prior exact placement service/HTTP tests passed replay and conflicting-key cases, while the new filtered concurrent-placement GameTest passed one local admission/one authority mutation/one entity; result: retries replay and distinct simultaneous operations converge through local gating plus durable authority CAS; limitation: live MariaDB deadlock/lease recovery remains staging.
- [x] Implement recall calendar-period transaction. — Evidence: changed `PetTransitions`, recall wire/service/repository paths, V002 replay metadata, authenticated HTTP handler/client, and JDBC/in-memory atomic adapters; exact `PetRecallServiceTest` passed 1/1 for UTC `2026-08`, first-use commit, second-use denial with `2026-09-01T00:00:00Z`, exact operation replay, and spawn-failure compensation releasing the period; result: recall authority revision and the nullable consuming-period key commit/compensate together; limitation: V001+V002 still require live MariaDB transaction/race verification and admin reset is open.
- [x] Unit-test all transitions and races. — Evidence: focused transition suites cover place/pickup/compensation, transfer prepare/claim/expiry, recall/compensation/month reset, admin recovery, stale entity decisions, and timestamp/version failures; exact adoption and placement concurrency tests passed, and the new filtered GameTest held one lookup open while a simultaneous request made zero extra reads/mutations and spawned exactly one representation; limitation: live MariaDB/multi-process races and fault injection remain staging acceptance.

### Phase C — Fabric physical pet MVP

- [x] Identify/mark pet cats and wolves without affecting ordinary mobs. — Evidence: changed `pet-fabric` identity/controller mixins and added testmod `PetPhysicalGameTests`; `.\gradlew.bat :pet-fabric:runGametest` passed all 10 required tests, including marked cat/dog and ordinary cat/wolf controls; result: marker-gated behavior is isolated; limitation: deployed third-party-mod interaction remains untested.
- [x] Persist pet identity metadata on entities. — Evidence: changed `TameableEntityMixin` NBT read/write and controller restore; GameTest `identitySurvivesVanillaNbtRoundTrip` passed pet/owner/revision/sleep plus exact variant/scale and safety restoration; result: vanilla entity save/load round trip is exact; limitation: a full process/chunk restart test remains.
- [x] Spawn exact cat/wolf variant and exact scale. — Evidence: added `PetEntityFactory`, variant invokers, and fallback; GameTest `factoryMaterializesExactIdentityAppearanceAndSafety` spawned tabby/ashen entities and asserted registry IDs plus scale base/effective values; result: both species materialize exactly; limitation: visual hitbox testing at min/max scales is pending.
- [x] Apply invulnerability. — Evidence: changed controller and `LivingEntityMixin`; GameTest direct generic-damage calls returned false with unchanged health for marked pets while ordinary controls took damage; result: marker-scoped damage immunity works; limitation: cross-mod/custom damage sources need staging coverage.
- [x] Disable attack/target/breed/tame/vanilla teleport behavior. — Evidence: changed marked-only entity/mob/animal/tameable/cat/wolf mixins; GameTest asserted target/attack/owner-assist/breed/age/love/tame/owner/leash/teleport rejection and unchanged position; result: excluded vanilla mechanics are blocked; limitation: vehicle and conversion/container behavior still need explicit runtime tests.
- [x] Add owner-only right-click interception. — Evidence: added `interaction/PetInteractionRouter`/handler, registered `UseEntityCallback`, and routed cat/wolf mixin fallbacks; filtered `.\gradlew.bat :pet-fabric:runGametest -PgametestFilter=pet_companion_test:pet_physical_game_tests_marked_pet_interaction_is_owner_only_and_ordinary_mobs_pass_through --offline` ran exactly 1/1 passing test; result: owner interaction opened once, non-owner returned `FAIL`, and an ordinary cat returned `PASS`; limitation: deployed permission/provider coexistence remains.
- [x] Add custom follow goal with adaptive speed. — Evidence: added `PetFollowOwnerGoal`/`PetPhysicalConfig`; 2 JVM config tests verified near/medium/far speed bands, and GameTest `petFollowsRegisteredOwnerWithoutTeleportOrForcedChunks` used a registered owner and observed distance closing over 60 ticks without teleport-like steps; result: marked owner-follow navigation and adaptive configuration work; limitation: runtime coverage is currently cat/default-distance only, with wolf and all distance bands still pending.
- [x] Add stuck detection and path recalculation. — Evidence: changed `PetFollowOwnerGoal`, added `PetStuckDetector` and one focused test; `.\gradlew.bat :pet-fabric:test --tests com.silver.aipets.fabric.ai.PetStuckDetectorTest --offline` ran exactly 1 test with zero failures; result: real progress resets the window, six negligible 10-tick samples trigger a navigation stop followed by the goal's fresh path request, with no teleport path; limitation: the Section 22 real-obstacle test remains pending.
- [x] Implement safe placement. — Evidence: added `SafePlacementFinder`, async `PetAuthorityGateway`, and `PetPlacementCoordinator`; GameTests rejected lava/void/solid/entity collisions, found a loaded nearby alternative, proved authority commit occurred before spawn, stamped the committed revision, and exactly compensated a forced spawn failure; result: safe commit-first placement passed in the real server world; limitation: authenticated HTTP gateway/config and `/pet place` command wiring remain.
- [x] Implement proximity pickup. — Evidence: added `PetPickupCoordinator` with backend/dimension/entity/revision/distance checks and commit-before-discard ordering; GameTest placed then picked up a marked entity through a 4-block authority CAS and asserted `HELD` plus entity removal; result: local proximity pickup works; limitation: authenticated HTTP gateway, speech-display cleanup, and `/pet pickup` command wiring remain.
- [x] Implement entity-load reconciliation. — Evidence: changed Fabric initializer/gateway/config plus `reconciliation/*` and shared strict wire codec; focused `HttpPetAuthorityGatewayTest` passed 1/1 authenticated loopback transport test, and the filtered reconciliation GameTest passed 1/1 through `ENTITY_LOAD` plus a bounded one-entity periodic scan; result: exact entities are asynchronously reused/refreshed over a configurable service boundary with duplicate in-flight suppression; limitation: deploy the service runner/database and complete process-restart staging.
- [x] Implement stale entity self-discard. — Evidence: changed `EntityAuthority` and `PetEntityReconciler`; the same 1/1 filtered GameTest spawned a wrong-UUID representation and observed immediate discard plus a structured reason, while the 2-test authority suite covered other mismatch decisions; result: conclusively stale loaded entities self-discard; limitation: service failure intentionally leaves entities intact and remote-recall/offline-backend staging is pending.
- [x] Handle server shutdown/restart and unloaded chunks. — Evidence: added `PetEntityRecoveryCoordinator` lifecycle wiring for join, bounded five-second online-owner checks, shutdown clearing, loaded-only dimension/chunk resolution, stale-candidate reconciliation, final authority reread, and exact-UUID reconstruction; one filtered GameTest passed 1/1 through three fresh coordinator instances and an unloaded remote chunk; result: missing local entities reconstruct once while unloaded chunks defer without tickets/loads; limitation: actual process kill/restart and corrupted-region staging remain.
- [x] Verify no forced chunk loading. — Evidence: audited Fabric main placement/reconciliation/recall paths and found no chunk ticket/force/load calls; passed placement/follow/reconciliation tests plus recall's loaded-world entity lookup use only already-loaded context; result: current physical lifecycle and recall do not force chunks; limitation: long-duration deployed observation and future transfer paths remain.
- [x] Verify physical features work while subscription inactive. — Evidence: authoritative snapshots carry `aiAccessEnabled`; exact place/pickup, compass, and recall GameTests passed with access false, including materialization, HELD pickup, compass refresh, and monthly recall; result: physical authority paths do not consult AI entitlement; limitation: actual Stripe webhook convergence remains staging work.

### Phase D — Commands and compass

- [x] Register `/pet` root/status help. — Evidence: changed snapshot wire/service JDBC access, Fabric `PetCommands`/gateway, and the command GameTest; the exact filtered status GameTest passed 1/1 for player-only help and one authoritative status lookup reporting name, species, HELD, sleep, and inactive AI access; result: root help now includes implemented adoption/place/pickup/recall/compass commands; limitation: dedicated UIs and admin operations remain pending.
- [x] Implement adoption command/UI with active-subscription check. — Evidence: added vanilla clickable private feedback in `PetAdoptionMenu`, menu routes for `/pet adopt`, `/pet adopt cat`, and `/pet adopt dog`, and retained the existing subscription-gated name submission; the exact filtered adoption GameTest passed 1/1, proving menu actions submit no adoption, populate the dog name command, deny inactive adoption, create once, and do not reroll; limitation: no client resource-pack screen is required by the current command/UI adaptation.
- [x] Implement `/pet place`. — Evidence: changed shared mutation wire types, service atomic idempotency/JDBC boundary, authenticated handler/client, runtime coordinator installation, `PetCommands`, and tests; the exact HTTP test passed 1/1 for applied/replayed/conflicting place plus compensation, and the filtered command GameTest passed 1/1 for commit-before-spawn and success feedback; limitation: JDBC behavior still needs live MariaDB verification.
- [x] Implement `/pet pickup`. — Evidence: changed the same authority/command path; the exact HTTP test passed 1/1 for applied and exact-replayed pickup, while the filtered command GameTest passed 1/1 for proximity pickup with AI access inactive, entity removal, HELD state, and exactly two revisions; limitation: deployed service/MariaDB staging remains.
- [x] Implement `/pet recall` with UTC monthly period and clear feedback. — Evidence: changed `PetRecallCoordinator`, gateway/client, `PetCompanionMod`, and `PetCommands`; the exact HTTP recall test passed 1/1 and the filtered recall GameTest passed 1/1 for safe destination spawn, fresh UUID, stale loaded-source discard, success feedback, and second-use `Next recall` feedback; result: async commit-first recall works without subscription gating; limitation: offline remote-backend and live MariaDB staging remain.
- [x] Implement `/pet compass` issue/replace. — Evidence: changed Fabric compass package, `PetCommands`/`PetCompanionMod`, config/deploy examples, and tests; the exact filtered compass GameTest passed 1/1 for HMAC-bound issuance/refresh, tamper rejection, and one owner/pet compass, while `\.\gradlew.bat :pet-fabric:test --tests com.silver.aipets.fabric.config.PetServiceClientConfigTest --offline` passed 1/1 for separate environment secrets, friendly names, redaction, and missing-secret rejection; limitation: rotating the shared signing secret invalidates old items until reissued.
- [x] Implement same-server/dimension pointing. — Evidence: changed `PetCompassManager`/item components and the same exact filtered GameTest passed 1/1, matching the lodestone target first to authoritative coordinates and then to the loaded marked entity's current block; limitation: client-rendered needle behavior still needs staging visual inspection.
- [x] Implement other-server/dimension/held status. — Evidence: changed compass presentation/config mapping and the same exact filtered GameTest passed 1/1 for `Held by you`, friendly `Survival Realm`, and `minecraft:the_nether`, with misleading lodestone targets absent remotely/cross-dimension; limitation: all deployed backend friendly names must be configured.
- [x] Enforce owner-bound inventory behavior and duplicate cleanup. — Evidence: changed compass manager plus item-entity/slot/hopper mixins and the same exact filtered GameTest passed 1/1 for duplicate/tampered removal, wrong-player/container rejection, hopper blocking, dropped-item deletion, and full-inventory no-drop behavior; limitation: third-party inventories still require staging compatibility checks.
- [x] Implement permissions. — Evidence: added Fabric `PetPermission`/`PetPermissions`, gated root/status/place/pickup with `aipets.use`, adoption/compass/recall with their explicit nodes, owner interaction with `aipets.chat`, and filtered help; the exact permission GameTest passed 1/1 with allowed status/compass help plus denied adopt/recall absent from help and making zero authority calls, while the exact interaction GameTest passed 1/1 with denied chat making zero handler calls; result: explicit player/admin nodes have vanilla level fallbacks and an installable provider boundary, while chat denial does not revoke physical commands; limitation: the deployment's selected permission-provider mod still needs a small adapter installed through the boundary.
- [x] Implement admin inspect/recover/reconcile operations. — Evidence: changed common recovery/reset contracts, service CAS/JDBC/admin HTTP paths, Fabric authority/client/commands, runtime registration, and operator docs; exact `AdminRecoveryTransitionTest` and `RecallAdminServiceTest` passed 2/2, Fabric compilation passed, and manual route audit confirmed reconcile calls `scanLoadedNow` for every world while nginx exposes no `/v1/admin/*`; result: permission-separated inspect, loaded-only reconcile, identity-preserving recovery-to-HELD, and current-UTC-month recall reset are available; limitation: deployed permission-provider, live MariaDB, and multi-backend operator drills remain.

### Phase E — Velocity presence and transfers

- [x] Add network login/logout presence reporting. — Evidence: added standalone `pet-velocity`, timestamped strict presence wire types, asynchronous authenticated client, per-owner serialization, `PostLoginEvent`/`DisconnectEvent` listeners, `/v1/presence`, and runtime sleep-service wiring; exact proxy-order and HTTP lifecycle tests passed 2/2 and the bundled 416,705-byte plugin JAR built; result: true proxy connect/disconnect reaches authoritative persisted presence without blocking Velocity; limitation: install/restart on the deployed proxy remains staging work.
- [x] Ensure backend switches do not count as logout. — Evidence: `PetCompanionVelocityPlugin` subscribes only to proxy initialization, post-login, true disconnect, and shutdown—no `ServerConnectedEvent`, pre-connect, or backend-switch event—and the existing admission/portal plugin is untouched; result: moving among gameplay/lobby backends emits no absence event; limitation: verify coexistence on the deployed proxy version.
- [x] Hook existing portal transfer before source disconnect. — Evidence: changed `MCServerPortals/ServerPortalsMod` plus `PetCompanionMod`; source compilation passed and source-order audit confirmed the bounded pet future completes before the unchanged signed `portal_request` is sent; result: the supported portal path prepares carry before disconnect; limitation: paired staging deployment remains.
- [x] Check authoritative pet entity and carry radius. — Evidence: `PetTransferCoordinator` validates owner/pet/revision/backend/dimension/entity UUID and 16-block 3D range; exact GameTest passed 1/1 for loaded near/far entities and the service lifecycle test independently rejected out-of-range preparation; limitation: modded portal paths need staging.
- [x] Perform source CAS `PLACED -> TRANSFERRING` and discard the source entity. — Evidence: exact GameTest passed 1/1, observing the physical entity present inside the authority call, persisted state `TRANSFERRING`, then removal; result: discard cannot precede successful CAS; limitation: live HTTP response-loss observation remains.
- [x] Persist transfer ID, source identity, final destination, start time, and expiry. — Evidence: strict codec/JDBC bindings plus exact service test round-tripped and persisted the full `TransferMetadata` aggregate; result: five-minute reservations survive process memory; limitation: repeat against live MariaDB.
- [x] Do not spawn in waiting lobby by default. — Evidence: exact GameTest used backend `waiting-lobby`, received `DESTINATION_NOT_RESERVED`, made zero mutations, and spawned nothing; exact service test also rejected the wrong destination; limitation: deployed lobby backend ID must not equal a gameplay ID.
- [x] Claim the matching transfer ID on the final backend and place exactly once. — Evidence: exact GameTest claimed on the final backend, reproduced exact identity/variant/scale, then a duplicate join made zero mutations; service test rejected a wrong ID and a second claim; limitation: cross-process HTTP response-loss recovery still needs staging.
- [x] Expire incomplete transfers safely to `HELD`. — Evidence: exact service test recreated the placement service/worker over the same persisted repository after the deadline, scanned one reservation, returned it to `HELD`, then scanned zero; runtime runs bounded scans immediately/every five seconds; limitation: live MariaDB scheduler observation remains.
- [x] Handle transfer failure, proxy restart, destination offline, and player disconnect. — Evidence: persistent expiry worker is proxy-independent; exact service test simulated a fresh service object and exact post-claim spawn compensation to `HELD`; no destination claim/disconnected owner naturally leaves the reservation for the same tested expiry path; limitation: kill/restart and response-loss staging remain.
- [x] Verify internal admission bypass remains unchanged. — Evidence: `Velocity-Wakeup-Lobby` was not edited; source audit confirmed ServerPortals still emits the same signed protocol payload after preparation, and `MCServerPortals compileJava --offline` passed; result: StickyRouter/internal one-shot admission logic is unchanged; limitation: live coexistence test remains.
- [x] Verify pet left behind remains discoverable by compass. — Evidence: exact transfer GameTest kept the far pet `PLACED` with its source entity; the already-passed compass GameTest resolves any remote `PLACED` state to friendly server/dimension status; result: far pets remain authoritative and locatable; limitation: combined two-backend UI staging remains.

### Phase F — Conversation UI and speech display

- [x] Reuse/adapt existing villager text input UI. — Evidence: added `PrivateChatPetTextInputUi` and chat capture/suppression mixins; the one filtered conversation GameTest passed 1/1, opening private input, capturing one message, and marking it suppressed with zero AI on open; limitation: deployed two-mod coexistence still needs staging.
- [x] Bind interaction session to owner UUID, pet ID, backend, and expiration. — Evidence: added `PetConversationSession`/registry; exact `PetConversationSessionRegistryTest` passed binding, replacement, correlation, and 30-second expiry; limitation: none in the tested in-process registry.
- [x] Validate distance/state on message submission. — Evidence: `PetConversationCoordinator` rechecks marked entity/owner/entity UUID/world/distance/awake plus authoritative placed backend/dimension/subscription; the filtered GameTest moved the owner away after UI open and observed zero dialogue calls, then accepted the valid retry; limitation: remote backend switch needs multiplayer staging.
- [x] Add inactive-subscription and sleeping feedback. — Evidence: coordinator/router added deterministic messages; the filtered conversation GameTest executed inactive access with no UI/model call, the existing sleeping-interaction GameTest executed sleeping denial, and service dialogue tests retained zero model calls; limitation: client styling is not visually staged.
- [x] Add async service request with timeout/correlation ID. — Evidence: added correlated request/response records plus `AsyncPetDialogueDispatcher`; the three-test focused JVM command passed off-caller-thread invocation, a real 30 ms timeout, and stale-correlation rejection, while the GameTest accepted exactly one matching reply; limitation: production HTTP dialogue adapter remains unwired.
- [x] Create one Text Display per active reply. — Evidence: added `PetSpeechDisplayManager`; the filtered GameTest created one display and proved a new reply removed/replaced the old one with active count one; limitation: none in the tested server state.
- [x] Set position above scaled mob. — Evidence: display manager uses effective `pet.getHeight()` plus configured gap; the filtered GameTest asserted the exact Y position on a 0.67-scale pet; limitation: final visual gap tuning remains staging work.
- [ ] Set fixed orientation to pet facing direction and visually confirm yaw. — Partial: the filtered GameTest asserted `BillboardMode.FIXED`, pet yaw +180°, and updated yaw after turning; the handoff explicitly requires visual confirmation, which headless testing cannot provide.
- [x] Update display position/orientation while visible. — Evidence: END_SERVER_TICK calls display `tick`; the filtered GameTest moved/turned the pet and asserted updated X/Z/yaw; limitation: client interpolation appearance remains unstaged.
- [x] Sanitize, wrap, cap, and time reply text. — Evidence: added `PetSpeechPolicy`; exact focused JVM test passed removal of formatting/URL/control content, two-sentence/80-character cap, wrapping, and 5–12-second lifetime bounds; limitation: final readability needs visual staging.
- [x] Remove display on timeout/pickup/unload/shutdown/new reply. — Evidence: lifecycle events plus manager removal/clear were added; the filtered GameTest passed expiry, replacement, unload, and shutdown-clear assertions, while the existing pickup GameTest proves pickup discards the pet and therefore fires unload; limitation: process-kill cleanup is naturally handled by world teardown rather than a callback.
- [ ] Test nearby-player visibility. — Partial: display uses vanilla tracked entity spawning and configured positive view range; no real second client has verified rendered visibility yet.

### Phase G — AI dialogue and short-term memory

- [x] Add configurable model client abstraction. — Evidence: added `DialogueModelClient`, model/timeout limits, token-counter boundary, and typed usage response; exact `DialogueServiceTest` passed 3/3 with a queue-backed mock receiving the configured timeout/schema; limitation: production model/tokenizer selection remains open.
- [x] Add strict structured output schema. — Evidence: added exact no-extra-fields `DialogueStructuredSchema` plus independent strict `DialogueOutputCodec`; the focused test proved the schema reaches the model boundary and malformed missing-field JSON is rejected; limitation: provider-specific schema transport adapter remains.
- [x] Implement moderation/safety preprocessing. — Evidence: added composable `DialogueSafety` and deterministic normalization/control/formatting/URL/prompt-extraction guards; exact test rejected unsafe input with zero model calls; limitation: deployment community-policy/external moderation adapter remains.
- [x] Implement prompt builder with explicit token budgets/drop order. — Evidence: added model-tokenizer-counted relationship/short/long/recent caps and exact low→medium→long→turn total-drop order; focused prompt test passed below a 220-token hard cap while retaining core/current data; limitation: selected model's actual tokenizer must be installed.
- [x] Include identity, traits, mood, relationship summary, eligible short-term events, bounded long-term recall, recent turns, and current input. — Evidence: typed `DialogueContext` and prompt sections include every listed source; exact test retained authoritative identity/current/high memory/newest turn, excluded expired/old data, and limited long-term cards to three; limitation: JDBC context-loader orchestration remains.
- [x] Do not send full history. — Evidence: prompt builder admits only eligible budgeted snippets, up to three long-term cards and the last four/700-token turns; exact test supplied 10 turns/12 short/5 long entries and excluded old/expired markers; limitation: staging prompt telemetry remains.
- [x] Implement cooldown, daily cap, concurrency lock, global cost cap, and circuit breaker. — Evidence: added atomic `DialogueAdmissionController` with one active call/pet, global concurrency, UTC owner cap, cooldown, daily/monthly spend, billed failures, and circuit reopening; exact admission test exercised every denial/result; limitation: final numeric spend caps require telemetry/business choice.
- [x] Parse/sanitize reply. — Evidence: strict parser plus `DialogueOutputValidator` and `PetSpeechPolicy` remove formatting/control/URLs, cap characters/two sentences, and wrap display text; exact service and speech-policy tests passed; limitation: production-provider fixtures remain.
- [x] Validate importance and proposed deltas. — Evidence: schema/parser require exact importance and every integer trait/mood field within proposal bounds; greetings downgrade to LOW and LOW candidates clear; authoritative clamp occurs separately; exact test exercised oversized proposals; limitation: major deterministic-event policy remains future work.
- [x] Persist short-term event and usage. — Evidence: added transactional in-memory and `JdbcDialogueStateStore` paths for event/raw-retention fields and secret-free token/cost/status usage; exact test produced 4 events/5 usage rows including rejected output; limitation: live MariaDB transaction verification remains.
- [x] Apply bounded/audited deltas. — Evidence: both stores reapply temperament ±1/3 daily, relationship ±2/6 daily, mood ±10, final 0–100, and audit each nonzero trait change under a state lock; exact four-interaction test ended curiosity +3, attachment +6, content at 100, with three audits per checked trait; limitation: live MariaDB concurrency remains.
- [x] Display result only on valid current server/session. — Evidence: coordinator requires matching session/request/pet IDs and re-resolves owner/entity/backend/dimension/distance before display; registry test rejected a stale correlation without removing the live request and the filtered GameTest displayed one matching response only; limitation: live cross-backend switching remains unstaged.
- [x] Mock model calls in automated tests. — Evidence: `DialogueServiceTest.QueueModel` captured calls/schema/token usage and returned malformed plus valid structured responses; exact focused suite passed 3/3 without network/provider access; limitation: provider contract testing awaits adapter selection.

### Phase H — Sleep scheduler and consolidation

- [x] Implement Velocity-backed owner network presence. — Evidence: added the isolated Velocity 3.2 plugin, strict environment-only client config, no-redirect async HTTP gateway, unique absence sessions, three bounded retries, ordered per-owner publication, source timestamps, authenticated service ingestion, and bounded shutdown drain; exact reporter test proved online completes before queued offline and transient failures succeed on attempt three; result: Velocity is the sole producer and delayed/retried requests cannot regress persisted presence; limitation: exhaustion after three failures requires deployed alerting and the next presence event to reconcile.
- [x] Implement 30-minute logout sleep trigger. — Evidence: exact authenticated presence test published proxy online/offline, preserved the first absence session against duplicates and a stale online event, then a fresh scheduler at logout +31 minutes produced `SLEEP_STARTED_AFTER_LOGOUT`; result: the previously isolated persisted rule is now connected end-to-end through the production-shaped transport; limitation: scheduler observation can lag up to its 30-second poll and live MariaDB remains staging work.
- [x] Implement 23-hour forced-awake deadline. — Evidence: added `sleep/*` policy/transitions/stores/service/scheduler and runtime wiring; focused `PetSleepServiceTest` passed 2/2, including no transition at 23h-minus-1ms and forced sleep at exactly 23h; result: the bounded production scheduler advances persisted forced deadlines; limitation: live MariaDB timing remains staging work.
- [x] Implement one-hour sleep independent of physical placement. — Evidence: sleep state has no placement dependency and the same focused test passed exact start/end instants for both absence and forced cycles; result: presence changes do not cancel the fixed one-hour window; limitation: deployed clock/database observation remains.
- [x] Prevent repeated sleep cycles during one continuous absence. — Evidence: the focused fake-clock test advanced one absence sleep, completed it, then advanced ten days while retaining the same persisted absence session; result: no second cycle was produced; limitation: end-to-end Velocity event delivery remains open.
- [x] Disable movement/chat while sleeping. — Evidence: production `PetFollowOwnerGoal`/`MobEntityMixin` stop or refuse navigation while the persistent sleeping marker is set, and `PetInteractionRouter` consumes interaction without opening chat; added `sleepingPetStopsActiveFollowUntilAwake`, whose exact filtered GameTest passed 1/1 after observing active follow, zero meaningful movement plus idle navigation throughout sleep, and resumed follow after waking; result: both movement and chat are disabled only while sleeping; limitation: deployed server observation remains part of general staging.
- [x] Permit pickup while sleeping without timer reset. — Evidence: sleep state is independent from placement mutations, and existing GameTest `placementAndPickupCommitOrderingAndCompensation` sets the marked pet sleeping before a successful authoritative proximity pickup; result: pickup remains allowed and has no sleep-store write path; limitation: live-service timer continuity remains staging work.
- [x] Persist restart-safe sleep timers. — Evidence: `JdbcPetSleepStateStore` uses row locks and indexed due scans; the expanded real MariaDB acceptance test took an adopted pet offline, recreated `PetSleepService` over the same database row, and observed exact 30-minute `SLEEP_STARTED_AFTER_LOGOUT` with the same absence session; result: the persisted timer survives a fresh service instance; limitation: process-kill recovery and production MariaDB staging remain.
- [x] Create idempotent consolidation jobs. — Evidence: `consolidation/*Job*`, `SleepConsolidationTrigger`, in-memory/JDBC repositories, and immediate leased scheduler use `sleep-consolidation:<pet>:<cycle>`; exact focused tests replayed duplicate starts and resumed a retry through a fresh worker without a second job/card; result: 3/3 consolidation tests passed; limitation: live MariaDB lease races remain staging work.
- [x] Select daily candidates deterministically. — Evidence: `ConsolidationCandidateSelector` orders high then distinct medium then repeated-low themes by recency/UUID under 30-event/6,000-token caps and excludes retry/system noise; exact test reduced seven shuffled candidates to the expected three; limitation: theme grouping is deterministic lexical normalization, not semantic clustering.
- [x] Skip empty/unsubscribed consolidation calls. — Evidence: `ConsolidationWorker` completes empty/noise-only cycles and inactive-access cycles before the model boundary, preserving meaningful inactive events; exact test observed zero mock calls for both paths; limitation: subscription state still depends on trusted Stripe convergence.
- [x] Generate strict long-term memory cards and relationship summary. — Evidence: bounded prompt/schema, exact-field codec, and validator enforce at most 5 cards, 100 tokens/card, 250 summary tokens, exact traits/tags/source UUIDs, and cited-event term grounding; exact test rejected extra JSON and a foreign source UUID, then committed one grounded card/summary; limitation: production model/tokenizer selection remains open.
- [x] Validate/commit cards and bounded trait deltas transactionally. — Evidence: in-memory atomic adapter and row-locking `JdbcConsolidationRepository` commit pet-scoped cards/sources, summary version, ±1 temperament/±2 relationship with daily/final clamps, eight audits, secret-free usage, event states, and job success together; exact test verified +1/+2, eight audits, and two attempt-usage records; limitation: JDBC path is compile/package verified but needs live MariaDB rollback/concurrency testing.
- [x] Mark/prune short-term rows according to retention policy. — Evidence: consolidation repositories mark selected/discarded rows and clear prompt eligibility; added `retention/*` with configurable 1–30 day raw-text policy (default 7), UTC-daily idempotent leased jobs, bounded in-memory/JDBC redaction and expiry, retry scheduler, runtime wiring, and operations guidance. Exact `RetentionCleanupWorkerTest` passed 2/2 at the exact boundary while preserving summaries; config test passed default/custom/invalid bounds. Limitation: live MariaDB update-limit/lease behavior remains staging work.
- [x] Test provider failure without extending sleep. — Evidence: exact fake-clock workflow failed the first model call, retained the original `24:00Z` end, resumed the durable retry in a fresh worker, and completed sleep at exactly that instant; result: provider error category was sanitized and physical sleep timing was unchanged; limitation: live provider timeout behavior remains staging work.

### Phase I — Vector retrieval

- [x] Select pgvector/Qdrant/other after DB discovery. — Evidence: selected Qdrant for the first provider adapter after local MariaDB 11.4.10/schema discovery; added `QdrantVectorMemoryRepository` plus `docs/VECTOR_OPERATIONS.md` and updated `README.md`; compile-only focused test passed, and the adapter enforces fixed dimensions, cosine collection setup, pet payload filtering, upsert/search/delete, and bounded outage errors; result: provider decision and adapter boundary are implemented locally; limitation: Docker's Linux engine is unavailable on this workstation, so real Qdrant endpoint, backup, sizing, and staging verification remain open.
- [x] Add vector repository abstraction. — Evidence: added `VectorMemoryRepository`, typed documents/matches/queries, in-memory cosine adapter, JDBC relational boundary, and the selected Qdrant HTTP adapter; exact `MemoryVectorWorkflowTest` passed 2/2 and the Qdrant adapter compile test passed; limitation: real Qdrant endpoint/staging verification remains unavailable on this workstation.
- [x] Embed only long-term memory cards. — Evidence: `MemoryEmbeddingWorker.enqueue` and `VectorMemoryDocument` accept only validated `LongTermMemoryCard`; the reindex test rejected inactive cards, embedded five compact-card texts, and found no raw-dialogue input; limitation: consolidation must later create the authoritative cards.
- [x] Add idempotent embedding jobs and retry state. — Evidence: added durable/in-memory job stores with version+model SHA-256 keys, leases, attempts, exponential retry, success/failure/cancel states; exact workflow test proved 5 first attempts retry on outage, second attempts succeed, and full requeue creates zero duplicate jobs; limitation: JDBC concurrency needs live MariaDB.
- [x] Scope every query by pet ID. — Evidence: repository contract requires `petId`, in-memory adapter selects the pet namespace before cosine ranking, and retrieval rechecks pet ID before relational hydration; exact test indexed a higher-scoring second-pet card yet returned only the requested pet; limitation: the future provider adapter must preserve this contract.
- [x] Add metadata filters and similarity threshold. — Evidence: typed query bounds threshold to `[-1,1]` and metadata sizes, repository prefilters entity/location tags, and service revalidates both; exact test excluded wrong-location data at a `0.95` threshold; limitation: final provider metadata schema is pending.
- [x] Retrieve at most the configured small memory set. — Evidence: query/result invariants hard-cap 1–3 and both repository/service stop at the requested limit; exact test indexed five eligible cards and returned exactly three; limitation: prompt-token integration remains Phase G.
- [x] Do not add per-message LLM query rewriting. — Evidence: `MemorySearchQuery.deterministicSearchText` uses only normalized original text plus sorted metadata and exposes no rewrite/model hook; exact test observed one embedding input equal to that deterministic string; limitation: dialogue orchestration is not yet wired.
- [x] Degrade gracefully if vector service is unavailable. — Evidence: retrieval catches the typed vector outage and returns `vectorDegraded=true` with zero cards; exact test disabled the repository and received the bounded fallback without an exception; limitation: full conversation fallback awaits Phase G.
- [ ] Implement full reindex command from relational rows. — Partial: `MemoryReindexService` pages every active JDBC/in-memory relational card and idempotently queues version/model jobs; exact test scanned 5 cards over pages of 2 and replayed 5 existing jobs. `MemoryReindexHttpHandler` is authenticated, registered when hybrid vector configuration is enabled, and now feeds the bounded `EmbeddingScheduler`; focused HTTP test passed unauthorized rejection plus first/replay responses. Live MariaDB/Qdrant/OpenAI execution and restart recovery remain.

### Phase J — Stripe and website

- [ ] Inspect/reuse current Stripe code. — Blocked: no website/Stripe repository exists in the supplied workspaces; the new pet-service integration therefore cannot be reconciled with or replace unknown site code.
- [x] Implement secure UUID account linking if missing. — Evidence: changed common link transport, Fabric `/pet link`, service link/Checkout/JDBC paths, V003, and the approved handoff wording; exact focused tests passed 7/7 and proved a clickable 256-bit opaque URL, HMAC-only persistence, UUID-bound server resolution, altered/expired rejection, Stripe-session attachment, and digest idempotency; result: the player types nothing and the verified matching Checkout webhook consumes/binds the link; limitation: live MariaDB/Stripe test-mode delivery and proxy staging remain.
- [x] Configure subscription Price ID. — Evidence: changed `PetServiceConfig`, runtime, and `config/pet-service.env.example` to require/validate `PET_STRIPE_PRICE_ID` only when Stripe is enabled and omit it/secrets from rendering; exact focused config test passed and repository-wide `compileJava` succeeded; result: entitlement accepts only the configured `price_*`; limitation: production/test Price values remain deployment secrets/choices.
- [x] Create hosted Checkout Session in subscription mode. — Evidence: added bounded `StripeHttpCheckoutClient`, `CheckoutLaunchService`/public handler, validated secret/public-origin config, exact nginx route, and success/cancel pages; exact tests passed a server-derived UUID/link hash/configured Price into a subscription-mode form, reused one idempotency key on retry, and kept redirect pages non-authoritative; Fabric compilation passed; result: valid links receive HTTP 303 only to `checkout.stripe.com`; limitation: actual Stripe test-mode API/CLI and TLS proxy validation remain.
- [x] Add customer portal if current site supports it. — Evidence: added trusted SQL customer lookup, bounded Stripe Portal client/service/private handler and wire transport, Fabric gateway plus clickable `/pet portal`, inert return route, and nginx/docs updates; exact `CustomerPortalServiceTest` passed 2/2 and `:pet-fabric:compileJava` passed; result: a connected player can manage/cancel billing without entering or seeing an identity/code, while unlinked players and unsafe URLs are rejected; limitation: Stripe test/live Portal configuration and real hosted-session/webhook staging remain.
- [x] Verify webhook signatures. — Evidence: added `StripeSignatureVerifier` plus raw-body `StripeWebhookHttpHandler`; exact focused billing test signed fixture bytes with HMAC-SHA256, accepted the valid `t`/`v1` header, rejected a changed byte, and enforced five-minute timestamp tolerance; result: parsing/database work occurs only after signature verification; limitation: live Stripe CLI delivery/TLS proxy remains staging work.
- [x] Store/process webhook events idempotently. — Evidence: added `JdbcStripeWebhookRepository` over `stripe_webhook_events` with digest conflict detection, transactional status/attempt updates, completed replay suppression, and retryable unresolved identity; exact focused test applied the same signed event twice and observed `DUPLICATE` with no second mutation; limitation: row-lock/deadlock behavior needs live MariaDB concurrency testing.
- [x] Handle lifecycle events and out-of-order updates. — Evidence: added strict normalization/reducer/JDBC convergence for Checkout, subscription create/update/delete, invoice paid/failed, and stale `created` rejection; exact six-fixture test plus lifecycle test passed and an older ACTIVE event could not regress newer CANCELED state; limitation: equal-second Stripe events converge by arrival because Stripe exposes only second-resolution `created` here, and live retry delivery remains staging work.
- [x] Derive AI access without deleting pet data. — Evidence: changed subscription reducer/policy and `JdbcSubscriptionAccess`; exact lifecycle test derived access from trusted status/Price/period/grace through cancellation while preserving the immutable owner UUID, and production SQL touches only billing ledger/subscription rows; repository-wide compile passed; result: physical pet tables/functions are not deleted or gated; limitation: live end-to-end database observation remains.
- [x] Test cancel-at-period-end, payment failure/grace, renewal, and reactivation. — Evidence: added `StripeBillingFlowTest` and six provider-shaped fixtures; exact focused test retained access for cancel-at-period-end, allowed configured three-day past-due grace then denied after it, restored ACTIVE access on `invoice.paid`, and disabled it on deletion; result: 4/4 billing tests passed; limitation: Stripe CLI/test-account staging is still required.
- [ ] Use Stripe test mode and fixtures for automated tests. — Partial: six `pet-service/src/test/resources/stripe` test-ID fixtures drive the exact 4/4 automated test, but no supplied Stripe test account/CLI credentials were available for an actual test-mode delivery.

### Phase K — Security, operations, and deployment

- [x] Add service authentication and firewall rules. — Evidence: changed service bind validation/config test plus `deploy/firewall` and exact-path `deploy/nginx` templates; focused config test rejected all four IPv4/IPv6 wildcard forms, source audit confirmed constant-time bearer checks on every internal handler and raw-signature-only webhook handling, and static template review found private-port allow/drop plus public 404 fallback; result: the application fails closed and deployable network rules expose only the signed webhook; limitation: actual host addresses/rules and `nft --check`/`nginx -t` require staging.
- [x] Move all secrets to approved configuration. — Evidence: changed `docs/SECRET_MANAGEMENT.md`, examples, and systemd hardening; source/config audit found service/database/Stripe/link/compass tokens only behind environment names or mode-restricted client files, no non-empty checked-in secret, and redacted config tests passed; result: inventory, file ownership, separation, and coordinated rotation are documented without JVM arguments/public chat; limitation: the operator must select/install the production secret store and future provider keys through the same boundary.
- [x] Add structured logs without raw secrets/conversation dumping. — Evidence: added shared `StructuredPetEvent` JSON events and converted every existing service/Fabric production log site, including startup/config, scheduler failures, dialogue model usage, reconciliation/discard, interaction, compass, NBT, compatibility repair, and worker failures; exact sanitizer test passed 1/1, both consumers compiled, and a source audit found no production `printStackTrace`, `System.out/err`, logged exception message, raw owner template, provider URL, prompt, or conversation field; result: logs carry safe operation/correlation/pet/backend/state/model/token/latency/count/outcome fields, hash owner UUIDs, and reduce failures to bounded class categories; limitation: deployed log shipping/retention and third-party logs remain operator concerns.
- [ ] Add metrics and health/readiness endpoints. — Partial: authenticated `/health/live`, `/health/ready`, and `/health/metrics` now expose process, migration, database/vector health and a fixed low-cardinality metric schema; `MetricsHttpHandler` records request count, aggregate latency, and errors; adoption creation increments CAT/DOG counters only for the transaction winner; embedding, recall, dialogue, memory retrieval, and consolidation scheduler boundaries now accept/record shared metrics. Exact `PetHealthHttpHandlerTest` passed 1/1, `PetAdoptionServiceTest` passed 6/6, `PetRecallServiceTest` passed with exact counter assertions, and focused dialogue/vector tests passed. Remaining minimum producers for subscription/state snapshots, variant buckets, Fabric stale/duplicate events, future transfer, and full production dialogue/consolidation runtime wiring must be connected before completion.
- [x] Add database backup coverage and restore instructions. — Evidence: added four `deploy/scripts` tools, hardened backup service/timer, safe config examples, and `docs/DATABASE_BACKUP_RESTORE.md`; exact Git Bash syntax plus mocked end-to-end self-test passed backup/checksum, tamper rejection, confirmation refusal, restore, 16-table/3-column/aggregate verification, and invalid-name rejection; result: consistent atomic dumps and guarded verified restores are deployable; limitation: real MariaDB credentials/version, encrypted off-host retention, timer monitoring, and a measured disposable restore drill remain host-side acceptance work.
- [x] Add vector reindex instructions. — Evidence: added and manually reviewed `docs/VECTOR_OPERATIONS.md` covering relational authority, post-selection invocation, safe reruns, status-only SQL monitoring, provider replacement, and no-delete rollback; result: the bounded/idempotent procedure is documented; limitation: the entrypoint/provider are still open.
- [x] Add systemd unit with restart, memory, user, working directory, and environment configuration. — Evidence: added `deploy/systemd/pet-companion.service`, service environment template, deployment guide, application distribution, and bounded runtime; `:pet-service:installDist :pet-service:distZip --offline` succeeded and a static audit found 8/8 required unit controls, 4/4 runtime libraries, 2/2 launchers, and zero test-secret leaks; limitation: `systemd-analyze`, installation, firewall, and staging remain host-side work.
- [x] Ensure AI service failure cannot crash Minecraft servers. — Evidence: changed `AsyncPetDialogueDispatcher` so timeout, gateway failure, null stage, and synchronous executor rejection all complete a future exceptionally for the coordinator's server-thread fallback; exact dispatcher test passed 1/1 including timeout and rejected-executor paths; result: AI scheduling/provider failure does not escape synchronously onto the tick thread; limitation: deployed outage/forced-shutdown staging remains open.
- [x] Add global AI-disable switch preserving physical pets. — Evidence: added strict environment-only `PET_AI_ENABLED`, runtime authority gating, dialogue/consolidation defense-in-depth gates, and deployment guidance; focused config/dialogue tests passed 4/4 and the runtime audit confirms adoption receives raw subscription access while only the snapshot AI bit is globally gated; result: `false` makes zero dialogue model calls and physical authority/adoption/mutation paths remain wired; limitation: changing the environment value requires a service restart.
- [x] Document deployment/rollback per component. — Evidence: expanded `deploy/README.md` with explicit service, Fabric backend, MariaDB schema, and vector rollback boundaries plus kill-switch recovery; manually checked that each preserves relational authority/data and requires post-rollback health/reconciliation; result: every currently defined deployable component has a non-destructive rollback path; limitation: commands require host-specific paths/backups and staging proof remains open.
- [ ] Build and deploy to a staging backend before all six servers.

## 22. Test plan and acceptance criteria

### 22.1 Appearance tests

- [x] Creating a cat chooses a valid cat variant and in-bounds scale. — Evidence: changed `pet-service` randomizer/catalog and Fabric integration GameTest; 2,000 JVM samples plus 512 runtime-registry samples and all 11 configured cat IDs passed; result: every tested cat output was registered and within 0.55–0.80; limitation: datapack-added variants are not adoption candidates yet.
- [x] Creating a dog chooses a valid wolf variant and in-bounds scale. — Evidence: changed `pet-service` randomizer/catalog and Fabric integration GameTest; 2,000 JVM samples plus 512 runtime-registry samples and all 9 configured wolf IDs passed; result: every tested dog output was registered and within 0.50–0.78; limitation: datapack-added variants are not adoption candidates yet.
- [x] Repeated pickup/place preserves exact variant and size. — Evidence: extended the exact filtered placement GameTest through two complete place/pickup cycles; the second materialization passed `assertPhysicalPet` for `minecraft:tabby`, scale base/effective value `0.66`, pet/owner identity, and current revision; result: authority-held appearance was reproduced exactly after pickup; limitation: backend transfer and process restart remain separate criteria.
- [x] Server restart preserves exact variant and size. — Evidence: the filtered lazy-recovery GameTest created a fresh recovery coordinator over unchanged authoritative state after entity loss and `assertPhysicalPet` verified the reconstructed `minecraft:tabby` variant and exact `0.66` base/effective scale; result: restart reconstruction uses persisted appearance rather than randomization; limitation: repeat across an actual server-process restart with live MariaDB/chunk files.
- [x] Backend transfer preserves exact variant and size. — Evidence: exact filtered transfer GameTest passed 1/1 and `assertPhysicalPet` verified destination `minecraft:tabby`, scale base/effective value `0.66`, pet/owner identity, and committed revision; result: destination reconstruction uses authoritative appearance without reroll; limitation: real two-backend client staging remains.
- [x] Invalid/removed variant follows deterministic repair policy. — Evidence: added `DeterministicVariantFallback`/factory repair signaling; JVM tests plus GameTest `removedVariantUsesDeterministicRegistryFallback` passed two independent materializations with the same valid fallback and warning; result: no silent reroll; limitation: authoritative audited database repair is not wired.

### 22.2 Ownership and duplicate tests

- [x] Concurrent adoption requests create one pet. — Evidence: the exact `PetAdoptionServiceTest.concurrentAdoptionCreatesExactlyOnePersistedPet` command above passed 1/1 with 32 simultaneous requests yielding one CREATED, 31 EXISTING, and one repository row; limitation: repeat against live MariaDB before release.
- [x] Concurrent `/pet place` requests create one representation. — Evidence: changed `PetPlacementCoordinator`, `PetPlacementStatus`, command feedback, and one GameTest; the exact filtered headless test passed 1/1 after holding the first authority lookup open, receiving `ALREADY_IN_PROGRESS` for the simultaneous request, observing one lookup, one mutation, one authoritative UUID, and exactly one loaded marked entity; limitation: repeat against multiple real backends/live MariaDB.
- [x] Player cannot interact with/pick up another owner's pet. — Evidence: the existing owner-only interaction GameTest denied non-owner right-click, and the extended exact placement GameTest placed an intruder within pickup radius but returned `NO_PET`, made zero authority mutations, and left the owner's entity alive; result: both interaction and pickup resolve strictly through the initiating owner's UUID; limitation: deployed permission-mod compatibility remains staging work.
- [x] Stale source entity discards after remote recall. — Evidence: one exact filtered GameTest committed `offline-source -> gametest` recall while the source entity was absent, then loaded the old marked source representation; `PetEntityReconciler` returned `STALE_DISCARDED`, logged `DISCARD_BACKEND_MISMATCH`, and removed it while the recalled destination entity remained; limitation: repeat on two deployed backend processes.
- [x] Saved old entity loading after placement elsewhere discards. — Evidence: the same filtered GameTest loaded a marked old representation with the right pet/owner but wrong entity UUID beside the authoritative entity; recovery reconciled candidates, emitted `DISCARD_ENTITY_UUID_MISMATCH`, removed the stale entity, and retained the exact authoritative entity; limitation: offline remote-backend recall staging remains separate.
- [x] Service/backend restart never creates two authoritative entities. — Evidence: the same test instantiated a second fresh recovery coordinator while the exact entity existed; its reconcile-and-reread path returned `AUTHORITATIVE_ENTITY_PRESENT`, retained object identity, discarded a simultaneous stale candidate, and spawned no replacement; limitation: actual multi-process response-loss and restart staging remain.
- [x] Admin entity deletion does not delete pet identity. — Evidence: exact `AdminRecoveryTransitionTest` started from a persisted placed pet with its physical representation treated as absent, applied admin recovery, and asserted unchanged pet/owner IDs, name, appearance, traits, and mood with only placement becoming `HELD` and revision increasing; production recovery contains no delete/reroll path and queues loaded stale representations for authority discard; limitation: repeat the deletion/recovery workflow on a staged backend with live MariaDB.

### 22.3 Movement tests

- [x] Pet follows owner at near/medium/far distances. — Evidence: extended the exact filtered follow GameTest from a 5.5-block approach to a 21.5-block timeline and recorded real movement while the configured selector visited far, medium, and near bands; result: the marked pet traversed every band toward its registered owner without teleporting; limitation: deployed terrain variety and wolf parity remain staging coverage.
- [x] Pet accelerates enough to avoid routine separation. — Evidence: the same GameTest began beyond the 16-block far threshold, observed meaningful far-band movement, and reduced squared separation by more than 25 within 75 ticks; existing exact config test independently proves ordered speeds `1.0 < 1.25 < 1.5`; result: far-distance acceleration closes routine separation instead of allowing it to grow; limitation: no numeric production tick-time/load benchmark has been run yet.
- [x] No vanilla teleport occurs. — Evidence: changed `TameableEntityMixin`; GameTest invoked the marked entity's teleport path, asserted `shouldTryTeleportToOwner=false`, and verified unchanged position; result: vanilla owner teleport is canceled; limitation: custom follow pathing still needs distance/tick tests.
- [x] Pet does not attack or assist combat. — Evidence: changed `MobEntityMixin`/`WolfEntityMixin`; GameTest verified null target, failed direct attack, and failed wolf owner-assist predicate; result: marked pets cannot participate in combat; limitation: third-party combat hooks need staging coverage.
- [x] Pet does not breed/tame/change owner. — Evidence: changed animal/passive/tameable/cat/wolf mixins; GameTest verified age/love/breed/tame/owner mutations fail for marked pets and succeed for ordinary controls; result: vanilla ownership/reproduction paths are isolated; limitation: external direct-NBT editing is outside this guard.
- [x] Pet recalculates after ordinary path blockage. — Evidence: the extended GameTest enclosed the following pet in a real three-block-high stone cage for 70 ticks (beyond the 60-tick stuck timeout), verified the obstruction held it, removed the cage, and observed more than 0.5 blocks of subsequent movement; exact `PetStuckDetectorTest` also proves timeout/reset semantics; result: stopped navigation is refreshed and resumes after ordinary blockage clears; limitation: complex modded path nodes remain staging work.
- [x] No chunk-force-loading leak. — Evidence: `rg` source audit found no main-code chunk ticket/force/load call, while passed safe-placement, follow, and entity-load reconciliation GameTests asserted unchanged forced-chunk sets (follow also checked an unloaded remote sentinel); result: current physical lifecycle creates no force-loading leak; limitation: long-duration deployed observation and future transfer features remain pending.
- [x] Ordinary cats/wolves remain unchanged. — Evidence: GameTest `ordinaryCatsAndWolvesRetainVanillaBehavior` verified both unmarked species still age, love, tame, accept ownership, and take damage; result: all control assertions passed; limitation: broader vanilla behavior and modpack regression testing remain.

### 22.4 Placement/transfer/recall tests

- [x] Pickup fails beyond radius. — Evidence: GameTest `placementAndPickupCommitOrderingAndCompensation` moved the registered owner beyond the 4-block default, invoked `PetPickupCoordinator`, and asserted `OUT_OF_RANGE`, unchanged `PLACED` authority, and a live entity; result: all assertions passed; limitation: multi-dimension/backend command feedback remains pending.
- [x] Pickup works while sleeping. — Evidence: the same GameTest set both authority snapshot and entity sleeping flags before pickup, then asserted `PICKED_UP`, `HELD`, revision advancement, and entity discard; result: sleep does not block physical pickup; limitation: the future sleep scheduler/timer-reset invariant is not implemented yet.
- [x] Place fails if already placed anywhere. — Evidence: the exact filtered place/pickup GameTest issued `/pet place` twice before pickup; the second request left revision `1`, the original authoritative entity UUID, one live representation, and exactly one mutation call unchanged; result: any non-`HELD` authoritative pet is rejected before the placement endpoint; limitation: live multi-backend staging remains.
- [x] Near pet auto-carries through backend transfer. — Evidence: exact filtered transfer GameTest passed source reservation/removal and final destination claim/spawn for a loaded pet within 16 blocks; limitation: paired live backend/proxy staging remains.
- [x] Far pet remains on source. — Evidence: the same GameTest placed a live entity 20 blocks away and asserted `LEFT_BEHIND`, unchanged `PLACED` authority, and a live source entity; limitation: remote compass presentation was verified separately.
- [x] Manually held pet stays held through transfer. — Evidence: the same GameTest passed `HELD_UNCHANGED`, unchanged `HELD` authority, and no transfer mutation; limitation: live portal feedback remains staging.
- [x] Waiting lobby never accidentally materializes pet. — Evidence: the same GameTest passed a waiting-lobby join with `DESTINATION_NOT_RESERVED`, zero authority mutations, and no entity; limitation: deployed canonical backend IDs must be configured correctly.
- [x] Source state becomes `TRANSFERRING` before source entity discard. — Evidence: the same GameTest observed the source entity inside the prepare CAS, then `TRANSFERRING`, then removed; result: exact ordering passed.
- [x] Final backend can complete only the matching transfer ID once. — Evidence: service lifecycle test rejected wrong destination/ID and repeat CAS, while the GameTest's duplicate destination join made zero mutations after one exact placement; result: one reservation produced one representation; limitation: multi-process load staging remains.
- [x] Failed destination placement or transfer expiry leaves pet held. — Evidence: service lifecycle test passed exact post-claim compensation to `HELD` and restart-style expiry to `HELD`, with a second scan doing nothing; limitation: forced Fabric spawn failure is covered by the shared placement compensation path but needs deployed transfer staging.
- [x] One monthly recall succeeds; second fails with next date. — Evidence: exact service test passed first `2026-08` recall and denied the second with `2026-09-01T00:00:00Z`; exact filtered command GameTest independently displayed success and already-used `Next recall` feedback; limitation: repeat against live MariaDB before release.
- [x] Failed recall does not consume entitlement. — Evidence: exact service test committed a recall, compensated a simulated destination spawn failure to `HELD`, observed `COMPENSATED` on replay, then successfully recalled again in the same UTC month; result: failure clears the consuming key; limitation: the Fabric forced-spawn-failure branch and live JDBC transaction still need staging/integration coverage.
- [x] Recall from offline backend cannot later duplicate. — Evidence: the same test recalled with no source entity loaded, loaded the saved source entity twice through two fresh source reconcilers, discarded both, then ran a fresh destination recovery and counted exactly one live representation with the committed destination UUID; limitation: live service/MariaDB and real chunk-file staging remain.

### 22.5 Compass tests

- [x] Correct direction on same backend/dimension. — Evidence: `PetCompassManager` plus the exact filtered compass GameTest above passed 1/1 for exact authoritative and live-entity `GlobalPos` targets; limitation: visual client needle inspection remains.
- [x] Correct server name on different backend. — Evidence: configured friendly-name mapping plus the exact filtered compass GameTest passed 1/1 with `Survival Realm` lore and no target; limitation: deployed labels remain operator configuration.
- [x] Correct dimension/held status. — Evidence: the exact filtered compass GameTest passed 1/1 for held and Nether lore, with no misleading target; limitation: none in the tested vanilla paths.
- [x] Dropped/container-transferred item is blocked/removed. — Evidence: the exact filtered compass GameTest passed 1/1 through loaded item-entity, vanilla slot, wrong-player, and hopper guards; limitation: modded inventory compatibility remains untested.
- [x] Duplicate compass is removed. — Evidence: the exact filtered compass GameTest passed 1/1 after injecting a duplicate and a signature-tampered copy, leaving exactly one trusted item; limitation: periodic/deployed timing remains to be observed in staging.
- [x] Full inventory produces clear failure. — Evidence: the exact filtered compass GameTest passed 1/1 with every inventory slot full, asserting explicit `inventory is full` feedback, zero compass insertion, and no world drop; limitation: none in the tested player inventory.
- [x] Works after subscription cancellation. — Evidence: changed common/service/Fabric snapshots to carry authoritative `aiAccessEnabled`, and the exact HTTP read test plus exact filtered compass GameTest each passed 1/1 with access false while compass refresh/pointing remained available; limitation: Stripe webhook-driven cancellation still needs deployed staging.

### 22.6 Sleep tests with fake clock

- [x] Whole-network logout starts 30-minute timer. — Evidence: exact proxy reporter test created one offline absence after ordered online delivery, and exact HTTP/fake-clock lifecycle test persisted its source timestamp/session and started sleep only after the 30-minute threshold; result: the whole-network disconnect path is complete in code; limitation: deployed proxy/service/MariaDB staging remains.
- [x] Backend switch does not start logout sleep. — Evidence: source audit found no backend-connect/switch subscription in `pet-velocity`; only `DisconnectEvent` calls `ownerDisconnected`, while duplicate offline events cannot replace the first persisted absence; result: ordinary server/lobby changes leave network presence online; limitation: deployed coexistence with the existing routing plugin remains.
- [x] Sleep starts at 30 minutes absent. — Evidence: focused `PetSleepServiceTest` passed with persisted logout at `00:01Z`, no due transition before the deadline, and `SLEEP_STARTED_AFTER_LOGOUT` at exactly `00:31Z`; limitation: Velocity integration remains separately open.
- [x] Forced sleep starts at 23 hours awake. — Evidence: the same 2/2 focused test rejected 23h-minus-1ms and started forced sleep at exactly 23h; limitation: live MariaDB scheduler observation remains.
- [x] Sleep ends exactly one hour later. — Evidence: both fake-clock scenarios remained sleeping at end-minus-1ms and completed at the exact persisted end instant; limitation: scheduler polling can observe the due transition up to 30 seconds later in production.
- [x] Owner returning early does not cancel sleep. — Evidence: the forced-sleep test recorded an online presence update ten minutes into sleep, retained `sleeping=true`, and completed only at the original exact end; limitation: end-to-end Velocity delivery remains open.
- [x] Pickup/place does not reset sleep. — Evidence: the extended exact placement GameTest set authoritative/entity sleep before pickup, completed pickup and a second placement, and asserted the newly materialized entity remained sleeping; placement/pickup coordinators and authority transitions have no sleep-state mutation dependency; result: two physical state transitions advanced revisions without resetting sleep; limitation: live JDBC timer continuity remains covered by the separate restart/staging criteria.
- [x] Long offline absence triggers only one sleep/consolidation cycle. — Evidence: one absence cycle completed and a ten-day advance produced zero further transitions while the same absence-session flag remained consumed; limitation: consolidation itself is not implemented, so this verifies cycle issuance only.
- [x] Restart restores due timers without duplicate consolidation. — Evidence: existing fake-clock sleep test restored a persisted due timer through a new service, while the exact consolidation test replayed a duplicate cycle as `created=false` and resumed its leased retry through a fresh worker; immediate schedulers plus JDBC jobs persist both paths; limitation: process-kill testing against live MariaDB remains staging work.
- [x] AI failure does not extend sleep. — Evidence: first consolidation attempt threw from the mock provider at forced-sleep start; exact test retained the same sleeping state/end and observed `SLEEP_COMPLETED` exactly one hour later after a fresh-worker retry; limitation: live provider/network timeout staging remains.

### 22.7 Conversation/memory tests

- [x] Right-click opens existing/adapted text UI. — Evidence: the existing filtered interaction GameTest proved owner right-click reaches only the installed handler; the new filtered conversation GameTest proved that handler opens adapted private chat input only after authority/proximity/access validation; limitation: deployed Villager-Interface coexistence remains.
- [x] No AI call occurs until submission. — Evidence: the filtered conversation GameTest asserted zero dialogue calls after valid private-input open and exactly one after valid captured submission; limitation: production transport remains unwired.
- [x] Sleeping and inactive pets do not invoke AI. — Evidence: exact service tests observed deterministic zero-model denials; filtered Fabric interaction/conversation GameTests independently executed sleeping and inactive gates with zero dialogue calls/no input for inactive; limitation: client message styling remains unstaged.
- [x] Active subscription produces one bounded reply. — Evidence: the same exact test supplied `aiAccessEnabled=true`, received one strict/sanitized two-sentence reply per request, and observed exactly one model call/event; limitation: live subscription webhook convergence and display remain.
- [ ] Text Display faces pet direction, follows it, and expires. — Partial: the filtered GameTest proved fixed server yaw +180°, turn/position following, and bounded expiry; actual client visual confirmation of the forward-facing text surface is still required.
- [x] Malformed model JSON is rejected safely. — Evidence: exact test returned JSON containing only `reply`; service returned `INVALID_MODEL_OUTPUT`, persisted rejected usage, created no event, and did not expose model text; limitation: live provider malformed fixtures remain.
- [x] Low/medium/high expirations are correct. — Evidence: exact fake-clock test persisted LOW at +2h, MEDIUM at +8h, and HIGH with no expiry; limitation: cleanup/redaction scheduler remains.
- [x] Prompt builder stays below hard token cap. — Evidence: exact test injected excess short/long/turn context under a 220-token hard cap and asserted the assembled provider input was ≤220 while core/current fields remained; limitation: production tokenizer adapter remains.
- [x] Cross-pet memory retrieval is impossible. — Evidence: exact `MemoryVectorWorkflowTest` indexed matching cards for two pets, with the other pet's vector scoring higher, and returned only the requested pet; provider pre-ranking scope plus service pet/version/active revalidation provide defense in depth; limitation: selected production-adapter staging remains.
- [x] Empty day skips consolidation. — Evidence: exact consolidation workflow enqueued and completed an empty cycle with zero model calls and no memories; duplicate enqueue returned the same terminal job; limitation: live SQL inspection remains staging work.
- [x] Meaningful day creates bounded long-term cards. — Evidence: exact workflow consolidated high/medium events into one ≤100-token pet-scoped card with its exact source UUID, bounded summary/version, audited bounded traits, and one embedding enqueue; noise was discarded; limitation: generated-memory quality still needs production-provider evaluation.
- [x] Trait deltas obey per-event/daily/final bounds. — Evidence: four mock replies proposed +100 everywhere; exact test applied curiosity only +1 three times/+3 daily, attachment +2 three times/+6 daily, then zero, and clamped mood content to 100 with audits; limitation: live SQL race test remains.
- [x] Logout/inactivity never lowers relationship traits. — Evidence: the now-wired Velocity→presence→sleep path depends only on `PetSleepService`/`PetSleepStateStore`, which have no trait/mood repository access, while exact sleeping/inactive dialogue tests retained attachment/trust/security unchanged; result: logout can change presence/sleep only and cannot apply relationship deltas; limitation: direct database tampering is outside this invariant.
- [x] Vector outage still permits bounded conversation without long-term recall. — Evidence: exact vector workflow returns a degraded empty card set on outage, while exact dialogue workflow succeeds with an empty bounded long-term section using relationship/short/current context; limitation: selected production vector/dialogue adapters remain.

### 22.8 Stripe tests

- [x] Spoofed success redirect grants nothing. — Evidence: changed webhook/reducer and inert Checkout result-page paths; exact signed-fixture test asserted even trusted `checkout.session.completed` only binds IDs and leaves access false, while success/cancel GETs have no subscription mutation path; result: only later trusted subscription/invoice state grants access; limitation: public TLS proxy behavior still needs staging review.
- [x] Valid signed webhook updates subscription. — Evidence: changed signature/parser/service/reducer repositories; exact focused test HMAC-signed raw ACTIVE fixture bytes and observed `APPLIED`, ACTIVE, and access enabled; result: passed; limitation: JDBC update needs live MariaDB.
- [x] Duplicate webhook is idempotent. — Evidence: changed webhook event ledger/JDBC repository; exact focused test replayed the identical signed event ID/digest and observed `DUPLICATE` with two total unique processed payloads; result: no second state application; limitation: live concurrent duplicate delivery remains.
- [x] Out-of-order event does not regress newer status. — Evidence: changed `JdbcStripeWebhookRepository` stale guard and reducer; exact focused lifecycle test submitted older ACTIVE after newer CANCELED and observed `STALE` with CANCELED unchanged; result: passed; limitation: equal-second ordering remains arrival-based.
- [x] Cancel-at-period-end retains access until period end. — Evidence: changed entitlement policy/reducer/access reader; exact canceling fixture test observed `cancelAtPeriodEnd=true` and access true before the configured period end; result: passed; limitation: wall-clock live database expiry remains staging work.
- [x] Cancellation disables only AI and keeps all pet data/functions. — Evidence: changed only billing projection/access code; exact deletion fixture changed ACTIVE to CANCELED/access false while retaining UUID, and the existing inactive-access placement/pickup/compass GameTests already passed; result: no pet-data delete SQL/path exists; limitation: live Stripe-to-Minecraft staging remains.
- [x] Renewal restores AI immediately after trusted update. — Evidence: changed invoice reducer/JDBC convergence; exact lifecycle test moved PAST_DUE to ACTIVE/access true on `invoice.paid`; result: passed synchronously in the trusted webhook transaction; limitation: live provider delivery latency remains.
- [x] UUID mapping survives username change. — Evidence: changed link/Checkout/subscription transports and repositories to accept/store only `UUID ownerUuid`; exact link/launch test resolved the generated URL back to the same UUID with no username field anywhere; result: identity is username-independent and needs no website binding UI; limitation: live MariaDB/Stripe observation remains.

### 22.9 Load/cost tests

- [ ] Simulate expected concurrent players and chat requests.
- [ ] Server tick time remains unaffected by provider latency.
- [x] Database pool does not exhaust under pet heartbeats/status reads. — Evidence: `MariaDbAcceptanceTest` issued 32 concurrent `JdbcPetRepository.findByOwner` status reads through an 8-connection Hikari pool, awaited completion within 10 seconds, and asserted every read succeeded with no more than 8 total connections; result: the real MariaDB 11.4.10 pool remained bounded; limitation: production traffic volume and heartbeat cadence still require staging measurement.
- [x] Coordinate/compass behavior does not generate excessive writes. — Evidence: `PetCompassManager` contains only cached authority reads/local component updates, and the exact filtered compass GameTest exercised held/remote/dimension/authoritative/live updates while asserting zero authority mutation calls; result: no coordinate/compass database-write path exists; limitation: production read rate and pool load remain to be measured.
- [x] Global concurrency and spend caps work. — Evidence: exact admission test held one permit, denied another at global concurrency, recorded cost equal to the daily ceiling, then denied the next request; it also covered per-pet busy, cooldown, daily replies, and circuit open/reopen; limitation: production multi-process/global coordination and final cap values remain.
- [ ] Measure real average prompt/output sizes and estimated cost per active subscriber.
- [ ] Use results to set subscription price and final message quota.

### 22.10 Final release acceptance

- [x] One pet cannot duplicate across servers/restarts/recalls. — Evidence: combined exact placement-race, transfer one-shot, lazy restart, loaded-stale, and offline-recall GameTests each ended with exactly one authoritative physical representation; the newest test replayed an offline source save twice while preserving one destination object/UUID; result: local concurrency, transfer, restart reconstruction, and recall all converge through authority identity; limitation: multi-host response-loss/process-kill staging remains.
- [x] Exact appearance persists. — Evidence: adoption sampling, repeated pickup/place, backend transfer, recall, and now fresh-process lazy reconstruction GameTests all passed exact persisted species/variant/scale assertions; the new restart test twice reproduced `minecraft:tabby` at `0.66` without rerolling; result: every implemented physical materialization path consumes authoritative appearance; limitation: live MariaDB/process-upgrade staging and client visual confirmation remain.
- [x] No combat/pay-to-win behavior exists. — Evidence: existing exact physical GameTests directly verified marked pets reject damage, targeting, direct attacks, wolf owner-assist, breeding, taming, age/love, leash, and vanilla teleport while ordinary cat/wolf controls retain vanilla damage/mechanics; source re-audit found no weapon, damage, combat-stat, loot, or player-buff feature in production code; result: the implemented pet system is cosmetic/companion-only and cannot assist combat; limitation: third-party mods with invasive hooks still require staging compatibility testing.
- [x] Physical pet remains after subscription cancellation. — Evidence: the exact filtered placement/pickup GameTest above passed 1/1 with authoritative AI access false, including a live placed physical entity followed by owner pickup, and the compass GameTest passed with the same inactive state; limitation: end-to-end Stripe webhook cancellation still needs staging.
- [x] AI functions stop and resume correctly with subscription state. — Evidence: changed Stripe reducer/JDBC access plus the existing dialogue access gate; exact billing test disabled access after cancellation and restored it immediately on trusted renewal, while the existing exact dialogue test made zero calls when false and a bounded call when true; result: authoritative state drives the AI boundary without physical gating; limitation: live Stripe/MariaDB/provider staging remains.
- [ ] Sleep and consolidation are restart-safe. — Partial: exact fake-store tests now restore due sleep state, deduplicate cycles, and resume a consolidation retry through a fresh service object; JDBC row locks, unique keys, leases, and immediate schedulers implement the production shape, but a real process-kill/MariaDB recovery test is required for final release acceptance.
- [x] Prompts stay bounded. — Evidence: exact hard-cap/drop-order test plus provider-reported input-token guard ensure service input never relies on provider truncation; limitation: production model tokenizer and measured prompts remain staging requirements.
- [x] Long-term memories are pet-scoped and authoritative in SQL. — Evidence: `JdbcConsolidationRepository` inserts relational cards plus composite-pet source references inside the authoritative transaction and vector enqueue occurs only after commit; `MariaDbAcceptanceTest` inserted a real `long_term_memories` row and verified `JdbcLongTermMemoryStore.findActive` returns it for the owning pet but not a different pet; result: SQL pet scoping and authoritative lookup are proven against MariaDB 11.4.10; limitation: selected-vector provider reconciliation remains open.
- [x] OpenAI, vector, Stripe, and pet-service failures do not crash gameplay. — Evidence: hardened HTTPS billing-link validation, guarded all async command completions and compass reconciliation/scheduling, and made embedding/search/null-result/recall-executor failures degrade safely; exact `MemoryVectorWorkflowTest` passed 2/2 and the filtered eight-command outage GameTest passed 1/1 with zero mutations, unchanged authority, and a still-usable command dispatcher, combined with the prior exact OpenAI timeout/rejection dispatcher evidence; result: provider/service failures remain inside bounded fallback paths rather than escaping onto gameplay; limitation: real provider/network outage and forced server-shutdown staging remain.
- [ ] Existing admission queue, offline waiting, portal transfers, and Cultivation server remain unaffected.

## Phase Evidence Log

- 2026-09-05 — Local vector-provider selection (Phase I): selected Qdrant after reviewing the locally verified MariaDB schema and provider constraints; added `QdrantVectorMemoryRepository`, a pet-scoped Qdrant HTTP adapter using cosine collections and rebuildable payloads, plus local verification documentation and a disposable endpoint test. Verification: focused Qdrant test compiled and passed its no-endpoint-safe path; Docker was present but its Linux engine could not be started, so the real Qdrant integration test was not claimed. Result: provider decision and adapter are locally implemented. Remaining: real endpoint, embedding model/dimension, backup, sizing, worker wiring, and staging.

- 2026-09-05 — Qdrant runtime configuration boundary (Phase I/K): added validated environment settings for optional Qdrant enablement, origin, collection, fixed dimension, timeout, and API key; updated `config/pet-service.env.example`, `deploy/README.md`, and `PetServiceConfig`. Verification: focused `PetServiceConfigTest` passed 1/1, covering disabled defaults, normalized local URL, dimension/collection parsing, secret redaction, and credential-bearing URL rejection. Result: the provider can be configured without embedding secrets; limitation: endpoint/worker wiring remains staged work.

- 2026-09-05 — OpenAI embedding client and hybrid documentation (Phase I): selected OpenAI `text-embedding-3-small` at 1536 dimensions for embedding generation while keeping Qdrant and MariaDB self-hosted; added `OpenAiEmbeddingModelClient`, validated OpenAI/Qdrant configuration fields, environment/deployment examples, and updated vector/README architecture documentation. Verification: `OpenAiEmbeddingModelClientTest` passed 2/2 against an in-process HTTP server for request shape, bearer header, fixed-dimension parsing, and bounded HTTP failure; `PetServiceConfigTest` passed 1/1 for enabled hybrid settings and secret redaction; full build passed. Result: the hybrid provider boundary is implemented locally. Remaining: real OpenAI/Qdrant calls, model cost/quality measurement, and staging.

- 2026-09-05 — Embedding runtime wiring (Phase I/K): added `EmbeddingScheduler` and connected configured `JdbcLongTermMemoryStore`, `JdbcEmbeddingJobStore`, `OpenAiEmbeddingModelClient`, `QdrantVectorMemoryRepository`, `MemoryReindexService`, authenticated reindex HTTP context, and bounded 15-second worker scheduling to `PetServiceRuntime`; disabled configuration leaves the existing runtime unchanged. Verification: `:pet-service:test --offline` passed and `gradlew.bat build --offline` passed with 28 actionable tasks; no external provider call was claimed because Docker/Qdrant and API credentials are unavailable. Remaining: live MariaDB/Qdrant/OpenAI integration and process-restart staging.

- 2026-09-05 — Metrics producer wiring (Phase K): connected the shared runtime registry to transactional CAT/DOG adoption winners and the bounded embedding scheduler failure boundary; existing constructors remain source-compatible for isolated tests. Verification: `:pet-service:test --tests com.silver.aipets.service.adoption.PetAdoptionServiceTest --offline` passed 6/6, including one creation plus an existing-pet retry and exact CAT=1/DOG=0 Prometheus assertions. Result: two concrete feature producers now have evidence without claiming the broader metrics item complete. Remaining: subscription/state snapshots, variants, dialogue/consolidation/memory counters, Fabric events, recall, transfers, and database-derived gauges.

- 2026-09-05 — Recall metrics producer (Phase K): connected the shared runtime registry to recall attempts, successful/replayed outcomes, key conflicts, and thrown persistence failures while preserving existing constructor behavior. Verification: `:pet-service:test --tests com.silver.aipets.service.recall.PetRecallServiceTest --offline` passed with exact attempts=4, successes=4, failures=0 assertions across applied, unavailable, compensated replay, and next-period recall. Result: recall counters now have a concrete runtime producer; broader metrics item remains partial.

- 2026-09-05 — Dialogue/memory/consolidation metrics boundaries (Phase K): added source-compatible shared-registry overloads and bounded counters for model calls/outcomes/token/cost usage, retrieved memory cards, and consolidation scans/failures. Verification: focused `DialogueServiceTest` and `MemoryVectorWorkflowTest` passed together; full offline build passed after the changes. Result: the provider-neutral service boundaries now expose operational metrics without selecting or pretending to configure a production model/tokenizer. Remaining: runtime construction of the production dialogue/consolidation providers and the remaining Fabric/state producers.

- 2026-09-05 — Runtime construction cleanup (Phase K): made failed `PetServiceRuntime.create` close every scheduler already constructed, including optional embedding, before releasing the pool and datasource; this prevents non-daemon scheduler leaks during partial startup failure. Verification: `:pet-service:test --offline` passed and the full `gradlew.bat build --offline` passed with 28 actionable tasks. Result: partial-startup cleanup is bounded without changing normal startup/shutdown behavior. Remaining: staging fault injection is still required for deployed proof.

- 2026-09-05 — Authenticated vector reindex boundary (Phase I): added `MemoryReindexHttpHandler` for bearer-protected `POST /v1/admin/memory/reindex`, strict bounded page-size parsing, no-body enforcement, no-store responses, and sanitized failure statuses. Verification: `MemoryReindexHttpHandlerTest` passed unauthorized rejection, first enqueue counts, idempotent replay counts, and cache-control assertions using the real JDK HTTP server. Result: the operator-facing boundary is implemented and tested; limitation: runtime registration waits for the selected embedding model/worker configuration.

- 2026-09-05 — JDBC pool load check (Phase 22.9): extended `MariaDbAcceptanceTest` with 32 concurrent pet status reads through the configured eight-connection Hikari pool. Verification: `./deploy/scripts/local-mariadb.ps1 Test` passed with the disposable MariaDB 11.4.10 instance; all reads completed within 10 seconds and the pool reported no more than eight total connections. Result: the database-pool acceptance item is checked. Remaining: production heartbeat cadence and traffic-volume measurement.

- 2026-09-05 — Local MariaDB/JDBC and adoption UI completion (Phases B/D/H): added clickable private adoption menu feedback, portable localhost MariaDB runner/documentation, and `MariaDbAcceptanceTest`. Verification: official MariaDB 11.4.10 Windows ZIP SHA-256 `FB7C76F0804321EE373DAA49145F2056D2D88F321B614130ADEB05A1644EA003` verified; V001–V003 applied to disposable `aipets_test`; JDBC test passed 1/1 for 16 tables, invalid placement constraint rejection, eight-way concurrent adoption convergence, pet-scoped SQL memory lookup, and fresh-service persisted 30-minute sleep evaluation; filtered adoption GameTest passed 1/1. Result: five checklist items gained evidence. Remaining: production Pi schema/backup/network and selected vector-provider/full consolidation staging.
- 2026-09-01 — Sleeping movement completion (Phase H): added one focused runtime assertion over the existing marker-gated follow/navigation guards. Verification: `:pet-fabric:runGametest -PgametestFilter=pet_companion_test:pet_physical_game_tests_sleeping_pet_stops_active_follow_until_awake --offline` ran exactly 1/1 and passed after proving the pet first followed, stopped with idle navigation while sleeping, and resumed after waking; the existing exact owner-interaction test already proves sleeping chat does not open. Result: 1 Phase H item gained evidence with no production behavior change. Remaining: live restart-safe MariaDB timer staging is separate.

- 2026-09-01 — No-input hosted Checkout launch (Phase J): with owner approval, clarified the binding handoff so `/pet link` returns a clickable 256-bit opaque URL rather than a manually entered code; added HMAC-only resolution, server-derived UUID/Price metadata, outbound Stripe subscription Session creation, digest idempotency, V003 Session binding, transactional webhook consumption, inert result pages, and narrowly public/log-suppressed nginx routes. Verification: exact `StripeBillingFlowTest` plus `PetServiceConfigTest` passed 7/7, `:pet-fabric:compileJava` passed with the clickable `OpenUrl`, and updated backup syntax/mock restore passed; binding handoff SHA-256 is now `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 2 Phase J items gained evidence. Remaining: real Stripe test-mode/CLI delivery, live V003 MariaDB transaction, TLS nginx validation, and Customer Portal.
- 2026-09-01 — Hosted Customer Portal (Phase J): added `/pet portal` through authenticated Fabric/service transport, resolving only the verified SQL customer binding before server-side Stripe Session creation; exposed no UUID/customer/code to the browser and accepted only `https://billing.stripe.com` results. Verification: exact `CustomerPortalServiceTest` passed 2/2 with zero failures/errors and `:pet-fabric:compileJava` succeeded; binding handoff SHA-256 remained `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 1 Phase J item gained evidence. Remaining: configure/test Stripe Portal in both environments and stage hosted cancellation/webhook convergence through TLS.
- 2026-09-02 — Administrative recovery surface (Phase D/Section 22.2): added permission-separated inspect, loaded-only reconcile, idempotent recovery-to-HELD, and current-UTC-period recall reset while retaining identity, appearance, state history, and recall audit rows. Verification: exact `AdminRecoveryTransitionTest` and `RecallAdminServiceTest` passed 2/2 with zero failures/errors; service/Fabric production compilation succeeded; manual route audit confirmed every loaded world is queued and `/v1/admin/*` is absent from nginx; binding handoff SHA-256 remained `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 2 checklist/acceptance items gained evidence. Remaining: live MariaDB CAS/reset and deployed multi-backend/permission-provider drill.
- 2026-09-02 — Cross-service gameplay failure isolation (Section 22.10): guarded Fabric command/compass completion boundaries, rejected unsafe billing URLs before click-event creation, and broadened vector degradation through embedding/search/null-result and best-effort executor failure. Verification: affected common/service/Fabric production and testmod compilation succeeded; exact `MemoryVectorWorkflowTest` passed 2/2; filtered `external_service_failures_stay_inside_pet_commands` GameTest passed 1/1 across link, portal, status, adoption, place, pickup, recall, and compass with zero mutations, unchanged authority, and working root command afterward; prior exact dispatcher evidence covers OpenAI timeout and executor rejection; handoff SHA-256 remained `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 1 final acceptance item gained evidence. Remaining: deployed provider outages and shutdown races require staging.
- 2026-09-02 — Lazy restart/entity-loss recovery (Phase C/Section 22): added owner-online join/periodic recovery with loaded-chunk-only checks, stale-candidate reconciliation, a final authority reread, exact UUID/appearance reconstruction, in-flight suppression, and shutdown cleanup. Verification: one filtered headless GameTest passed 1/1, reconstructing twice across fresh coordinator instances, preserving `minecraft:tabby`/`0.66`, reusing the exact live entity, discarding a wrong-UUID saved entity, and leaving a remote sentinel chunk unloaded; production/testmod compilation succeeded and handoff SHA-256 remained `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 5 checklist/acceptance items gained evidence. Remaining: actual process/MariaDB/chunk-file restart staging and offline remote-recall loading.
- 2026-09-02 — Placement race/idempotency hardening (Phase B/Section 22.2): added an owner-scoped Fabric in-flight gate before entity preparation or authority I/O, with deterministic player feedback and automatic release on every completed outcome; durable service fingerprint/replay/CAS remains authoritative across processes. Verification: one filtered headless GameTest passed 1/1 with a deliberately pending first lookup, zero second lookup/mutation, one committed revision/entity UUID, and exactly one loaded representation; prior exact transition/idempotency/concurrency suites supply the storage/replay coverage, and handoff SHA-256 remained `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 3 checklist/acceptance items gained evidence. Remaining: live MariaDB and multi-backend concurrency/fault staging.
- 2026-09-02 — Offline-source recall reconciliation (Sections 22.5/22.10): added one focused acceptance scenario for the already-implemented authority reconciliation path. Verification: the exact filtered headless GameTest passed 1/1 after recall committed while `offline-source` had no loaded entity, two fresh source-process simulations independently discarded the later saved entity with `DISCARD_BACKEND_MISMATCH`, destination recovery reused the committed UUID/object, and the world contained exactly one marked representation; handoff SHA-256 remained `92D2544B49C55649C35A8194460AA1C9C779924DECA67F7C74D14B6836772A7A`. Result: 3 acceptance items gained evidence. Remaining: deployed two-backend/MariaDB/chunk-file fault staging.

- 2026-09-01 — Operational security and database recovery (Phase K): rejected wildcard service binds, added reviewed private-port nftables and exact-public-path nginx templates, documented every current secret/rotation boundary, and created consistent MariaDB backup/checksum/guarded-restore/structural-verification scripts plus a hardened daily timer. Verification: exact `PetServiceConfigTest` passed 1/1; all five shell files passed `bash -n`; mocked backup/restore self-test passed including tamper, missing-confirmation, invalid-database, and restored-schema guards; handoff hash remained unchanged. Result: 3 Phase K items gained evidence. Remaining: substitute real hosts/certificates, validate configs with Linux `nft`/nginx/systemd, install the chosen secret store, and complete an off-host disposable MariaDB restore drill.

- 2026-09-01 — Stripe entitlement and UUID-linking core (Phase J/§22.8): added environment-gated Price/webhook/link configuration, `/pet link`, HMAC-peppered expiring one-use token persistence and authenticated consumption, raw-body Stripe signature verification, transactional event ledger, six-event normalization, monotonic lifecycle reducer, grace/cancellation/renewal policy, and dynamic JDBC access expiry. Verification: exact `StripeBillingFlowTest` plus `PetServiceConfigTest` passed 5/5 total; `.\gradlew.bat build -x test` succeeded across all four modules (20 tasks, including Fabric test/testmod compilation and remapped JAR); fixture set covers Checkout/subscription/invoice lifecycle, and the handoff SHA-256 remained `F94300D0DB4C6355431A076755C06CD1B99D0C3E5552313DC4D182F6F3C15A37`. Result: 15 tracker/acceptance items gained evidence without trusting redirects or usernames. Remaining: website Checkout/Portal/account-session binding, actual Stripe test-mode/CLI delivery, and live MariaDB concurrency/staging.

- 2026-09-01 — Supported cross-backend carry (Phases B/E and §22): added strict idempotent prepare/complete/expire transport, Fabric commit-before-discard/commit-before-spawn coordination, exact final-backend gating, paired ServerPortals pre-hook, and restart-safe expiry scans. Verification: two focused JVM tests passed 2/2, one exact Minecraft transfer GameTest passed 1/1, affected Pet artifacts built, and MCServerPortals compiled. Result: 18 transfer/appearance checks gained evidence. Remaining: live MariaDB/two-backend/proxy staging, direct non-portal switch policy, and ambiguous response-loss observation.

- 2026-08-30 — Discovery baseline (partial Phase A): changed `IMPLEMENTATION_NOTES.md`, tracker, and Gradle/module scaffold. Verification: repository-wide `rg`/source inspection, Yarn jar `jar tf`/`javap`, and `.\gradlew.bat projects --offline` (`BUILD SUCCESSFUL`). Result: integration ownership and a compiling Gradle project graph established. Remaining: live/deployed versions, MariaDB operations, website/Stripe, staging, and visual/runtime API behavior.
- 2026-08-30 — Authority/physical foundation (partial Phases B–C): added common authority/domain types, service boundaries/in-memory concurrency adapter, SQL migration draft, Fabric entity factory/controller/mixins/follow goal, safe placement/pickup coordinators, and isolated testmod. Verification: user ran `.\gradlew clean build` after the lifecycle work (`BUILD SUCCESSFUL`, 23 actionable tasks); generated JUnit XML records 34/34 passing JVM tests with zero failures/errors, and `jar tf pet-fabric/build/libs/pet-companion-0.1.0-SNAPSHOT.jar` confirms lifecycle classes are present and testmod classes are absent. The earlier focused `.\gradlew.bat :pet-fabric:runGametest` passed 10/10 required headless tests, including navigation, collision safety, commit-before-spawn, exact compensation, and pickup. Result: full build matrix and marker-scoped physical lifecycle foundation verified. Remaining: live MariaDB/HTTP adapter, periodic reconciliation, multi-band/obstacle pathing, and staging.
- 2026-08-30 — Entity-load reconciliation (partial Phase C): added asynchronous loaded-entity reconciliation, event registration, structured stale-discard logs, monotonic revision checks, and filtered GameTest support. Verification: targeted `EntityAuthorityTest` passed 2/2; filtered headless GameTest command above ran exactly one test and passed 1/1 through the real entity-load hook, with an unchanged forced-chunk set. Result: exact entities are reused/refreshed and wrong-UUID entities discard without blocking the server thread. Remaining: authenticated gateway installation, bounded periodic/lazy checks, unload/restart staging, and remote-recall evidence.
- 2026-08-30 — Follow stuck recovery (partial Phase C): extracted the negligible-progress timer and kept recovery in the existing no-teleport goal. Verification: filtered `PetStuckDetectorTest` ran exactly 1/1 passing JVM test at the configured 10-tick sampling and 60-tick timeout. Result: progress resets the timer and a stuck window clears navigation before an immediate fresh path request. Remaining: real obstacle, small-scale, and both-species runtime checks.
- 2026-08-30 — Owner-only interaction/no-force-load audit (partial Phase C): added marker/owner-scoped interaction routing with a replaceable no-AI-on-open UI boundary. Verification: one filtered headless interaction GameTest passed 1/1; source audit plus passed placement/follow/reconciliation cases found no force/load calls and unchanged forced-chunk state. Result: marked owners route once, non-owners are blocked, ordinary entities pass through, and current physical paths stay loaded-context-only. Remaining: text UI/session/subscription feedback, restart/unload staging, and future-feature re-audit.
- 2026-08-30 — MariaDB repository adapter (partial Phase B): added `JdbcPetRepository`/sanitized persistence exception for transactional aggregate insert, row-locked CAS, exact placement/transfer mapping, and UTC `DATETIME(6)` conversion against V001. Verification: `.\gradlew.bat :pet-service:compileJava --offline` succeeded and a static placeholder audit found 23 insert/22 update parameters matching the bindings. Result: the production repository implementation is code-complete enough for live integration. Remaining: apply V001 to a disposable/live-compatible MariaDB, wire a bounded pool/driver, and run create/read/concurrent-CAS integration checks before completing persistence criteria.
- 2026-08-30 — Minimal packaging checkpoint: the latest `:pet-service:installDist :pet-service:distZip :pet-fabric:remapJar --offline` succeeded (14 tasks, no broad test rerun), producing a 1,373,464-byte runnable service ZIP, 91,360-byte service JAR, and 190,964-byte Fabric JAR with zero testmod entries. Result: current service, access wire, adoption endpoint, and mod are packaged. Remaining: live MariaDB and staging deployment.
- 2026-08-30 — Authenticated authority reads/periodic reconciliation (partial Phases B–C): added strict bounded pet JSON, bearer-authenticated read handler, timeout/no-redirect Fabric HTTP client, environment-only secret configuration, loaded-entity scan batching (4/tick every 1,200 ticks), duplicate in-flight suppression, and shutdown cleanup. Verification: exact `HttpPetAuthorityGatewayTest` passed 1/1 for owner/pet lookup, exact placed aggregate/sleep round-trip, 404, and bad-token rejection; filtered reconciliation GameTest passed 1/1 for load reuse, stale discard, one bounded periodic lookup, empty in-flight state, and unchanged forced chunks. Result: production-shaped read/reconciliation transport works asynchronously. Remaining: service executable/pool deployment, live firewall/TLS choice, and restart staging.
- 2026-08-30 — Initial command surface (partial Phase D): registered player-only `/pet` and `/pet status` with async authority lookup and server-thread response handling. Verification: the exact filtered command above passed 1/1, including help, one lookup, pet name, HELD placement, and sleeping state. Result: players have deterministic help/status feedback. Remaining: adoption, recall, compass, and deployed-service checks.
- 2026-08-30 — Idempotent placement command transport (partial Phases B/D): added an atomic operation ledger repository primitive, canonical fingerprints, exact stored-result codec, strict authenticated POST routes/client, and `/pet place`/`pickup` coordinator wiring. Verification: one exact service test passed for execute/replay/key-conflict with one revision; one exact HTTP test passed place/compensation/pickup replay and conflict; one filtered GameTest passed command placement/pickup 1/1. Result: physical commands are safe against client/network retries. Remaining: live MariaDB transaction verification before checking the general idempotency-storage item, plus deployable service/pool.
- 2026-08-30 — Standalone service runtime (partial Phase K): added validated environment-only configuration, Hikari pool capped at 16/default 4, bounded platform-thread HTTP execution, JDBC sleep/subscription reads, authenticated liveness/readiness with V001 table checks, graceful shutdown, runnable distribution, and deployment/rollback templates. Verification: focused config and health tests passed 2/2; offline distribution build passed; structural audit passed 8/8 unit controls, 4/4 libraries, 2/2 launchers, and zero test-secret leaks. Result: service can be installed as a constrained separate process and reports DB/migration readiness while optional AI/vector/Stripe remain degraded. Remaining: no local MariaDB/Docker engine was available, so live pool/JDBC, systemd syntax, firewall, backup, and staging evidence remain open.
- 2026-08-30 — Signed pet compass (Phase D): added HMAC owner/pet markers with a dedicated environment secret, async issue/refresh and 10-second cached-read reconciliation, friendly backend/dimension/held lore, authoritative/live lodestone targets, and vanilla item-entity/slot/hopper enforcement. Verification: the exact filtered compass GameTest above passed 1/1; `:pet-fabric:remapJar --offline` passed; artifact audit found 8 compass classes, 3 mixins, zero testmod entries, zero compass chunk-force references, and zero main/example test-secret literals. Result: all Section 22.5 criteria are now evidenced together with the subsequent inactive-access verification. Remaining: modded-inventory/client-visual and real Stripe staging.
- 2026-08-30 — Authoritative AI-access status (partial Phase D): extended strict snapshot JSON through JDBC subscription access, service handler, HTTP client, and `/pet status`; the exact read transport test passed 1/1 with `aiAccessEnabled=false`, the exact status GameTest passed 1/1 for species/sleep/placement/inactive-access output, and the same compass GameTest passed 1/1 while access was false. Result: subscription-gated AI state is visible without revoking the compass. Remaining: deployed Stripe cancellation convergence and other inactive physical actions remain staging work.
- 2026-08-30 — Adoption command transport (partial Phase D): added strict bounded adoption wire types, authenticated `/v1/adoptions`, runtime JDBC subscription/repository wiring, async Fabric client, and `/pet adopt cat|dog <name>`. Verification: exact concurrent service test, exact HTTP test, and exact filtered command GameTest each passed 1/1; retry returned the original pet without reroll, inactive access denied creation, and packaging passed. Result: slash-command adoption is operational. Remaining: dedicated adoption UI and live MariaDB concurrency.
- 2026-08-30 — Inactive-access physical lifecycle (Phase C): reused the authoritative access bit in the existing commit-safe command fixture; the exact filtered place/pickup GameTest passed 1/1 with access false, and the compass GameTest independently passed inactive refresh. Result: AI entitlement is not consulted by physical placement, pickup, or compass paths. Remaining: real Stripe cancellation convergence in staging.
- 2026-08-30 — Fabric secret/config boundary (partial Phase K): added a distinct persistent compass-signing environment variable, strict backend friendly-name parsing, redacted config rendering, and deployment rotation guidance. Verification: exact `PetServiceClientConfigTest` passed 1/1 and the production JAR contains no test secret literals. Result: bearer and signing secrets remain outside the properties file. Remaining: future AI/vector/Stripe secrets and deployed secret delivery are not yet covered.
- 2026-08-30 — Monthly recall workflow (partial Phases B–D): added service-time UTC periods, atomic consuming-key/JDBC workflow with separate exact recall/compensation fingerprints and V002 stored replay, V002-aware readiness, authenticated transport, safe Fabric destination placement, loaded stale-source invalidation, and `/pet recall`. Verification: exact service, HTTP (including compensation replay/key conflict), and inactive-access filtered GameTest commands each passed 1/1; minimal distribution/JAR packaging passed. Result: first use commits, second use reports the next UTC date, and compensated spawn failure releases entitlement. Remaining: live MariaDB concurrency, offline remote-source staging, forced Fabric spawn-failure staging, and admin-only reset/recovery.
- 2026-08-30 — Explicit permission boundary (Phase D): added all handoff player/admin permission nodes, vanilla level fallbacks, provider installation/restoration, per-command gates, and `aipets.chat` interaction denial independent of physical access. Verification: exact permission and interaction GameTests each passed 1/1. Result: denied feature nodes are hidden by Brigadier and never reach authority/UI handlers. Remaining: select and install the deployment-specific permission-provider adapter; admin operations themselves remain open.
- 2026-08-31 — Explicit adoption species allowlist (partial Phase B): added strict `PET_ALLOWED_SPECIES` configuration limited to `minecraft:cat`/`minecraft:wolf`, immutable runtime enforcement before creation, a distinct wire denial, and deterministic command feedback. Verification: focused `PetAdoptionServiceTest` and `PetServiceConfigTest` XML each report 1/1 with zero failures/errors; exact `HttpPetAuthorityGatewayTest.adoptionTransportEnforcesAccessAndReturnsExistingWithoutReroll` passed 1/1. Result: disabled species create no row, arbitrary entity IDs fail startup validation, and an existing pet is returned unchanged before requested-species validation. Remaining: live MariaDB verification; future species require reviewed domain/wire/catalog support.
- 2026-08-31 — Minimal permission/allowlist packaging checkpoint: `\.\gradlew.bat :pet-service:distZip :pet-fabric:remapJar --offline` completed successfully (13 tasks); artifact inspection found a 1,402,929-byte service ZIP and 219,152-byte Fabric JAR with zero testmod entries. Result: both small feature groups are present in deployable production artifacts. Remaining: this was intentionally a packaging check, not evidence for the 166 still-open acceptance items.
- 2026-08-31 — Authoritative sleep-timer foundation (partial Phase H): added fixed policy/state invariants, pure presence/due transitions, atomic store boundary with in-memory and row-locking MariaDB adapters, bounded restart-time/30-second scheduler, and sleeping chat denial. Verification: exact `PetSleepServiceTest` passed 2/2; exact sleeping-interaction GameTest passed 1/1; focused `:pet-service:distZip` and `:pet-fabric:remapJar` builds succeeded. Artifact inspection found 12 sleep classes in the 1,425,572-byte service ZIP/JAR distribution and zero testmod entries in the 219,307-byte Fabric JAR. Result: 30-minute absence, 23-hour forced, one-hour completion, early-return, and continuous-absence rules are deterministic and placement/provider independent. Remaining: Velocity presence transport, live MariaDB restart, explicit sleeping movement runtime test, and all consolidation work.
- 2026-08-31 — Provider-neutral vector memory foundation (partial Phase I): added validated compact-card/metadata types, relational and vector repositories, deterministic queries, authoritative hydration, durable leased embedding jobs with retry, outage fallback, full relational reindex orchestration, and operations guidance. Verification: exact `MemoryVectorWorkflowTest` passed 2/2 for 5-card paged/idempotent reindex, outage/retry/success, cross-pet isolation, metadata/threshold filters, top-3 cap, deterministic no-rewrite input, counters, and degraded fallback; focused service distribution build succeeded. Result: 10 checklist/acceptance items gained evidence without selecting a vendor. Remaining: provider/host decision, live JDBC concurrency, runtime adapter/worker, authenticated reindex entrypoint, and dialogue integration.
- 2026-08-31 — Bounded dialogue and short-term-memory core (partial Phase G): added configurable model/tokenizer contracts with mandatory JSON Schema, access/sleep/safety gates, deterministic budget/drop prompts, strict output validation/sanitization, atomic admission/cost/circuit controls, and in-memory/JDBC transactional event/usage/state/audit persistence. Verification: exact `DialogueServiceTest` XML reports 3/3 with zero failures/errors for excess-context hard caps, no-full-history selection, zero-call denials, malformed output, four bounded replies, exact retention, per-event/daily/final clamps, audit counts, and every admission denial; focused service distribution build produced a 1,555,589-byte ZIP with 44 dialogue classes and the placeholder/unsafe-logging audit was empty. Result: 21 checklist/acceptance items gained evidence. Remaining: production model/tokenizer/moderation configuration, live MariaDB concurrency, JDBC context loading/HTTP transport, and measured cap tuning.
- 2026-08-31 — Durable sleep consolidation core (Phase H): added deterministic high/medium/repeated-low selection, 6,000-token prompts, strict grounded output, idempotent leased jobs/retries, sleep-start trigger, immediate scheduler, secret-free attempt usage, atomic in-memory/JDBC card/source/summary/trait-audit/event commits, and embedding handoff. Verification: exact `ConsolidationWorkflowTest` 3/3 plus `PetSleepServiceTest` 2/2 passed with zero failures/errors; focused `:pet-service:distZip` succeeded, producing a 1,629,858-byte ZIP and 355,881-byte service JAR containing 33 consolidation classes; handoff SHA-256 remained `F94300D0DB4C6355431A076755C06CD1B99D0C3E5552313DC4D182F6F3C15A37`, and unsafe-marker audit was empty. Result: 11 checklist/acceptance items gained evidence without broad test expansion. Remaining: choose/wire production model/tokenizer and runtime scheduler, live MariaDB restart/rollback/concurrency, and provider quality/staging.
- 2026-08-31 — Bounded raw-dialogue retention cleanup (Phase H hardening): added 9 production files/584 lines for configurable retention policy, UTC-daily unique jobs, leases/retries, bounded atomic in-memory/MariaDB prompt expiry and raw redaction, hourly runtime scheduling, config/deployment validation, and `docs/RETENTION_OPERATIONS.md`. Verification: exact retention/config tests passed 3/3 with zero failures/errors; focused `:pet-service:distZip` produced a 1,658,111-byte ZIP and 386,521-byte JAR containing 14 retention classes; exact seven-day boundary redacted only raw fields while retaining summary/source identity, a fresh worker resumed an injected failure, handoff hash remained unchanged, and unsafe-marker audit was empty. Result: the prior raw-cleanup limitation is closed without adding schema or broad tests. Remaining: live MariaDB and staging observation.
- 2026-08-31 — Private conversation and speech lifecycle (Phase F): added Villager-style private chat capture/suppression, owner/pet/backend/expiry sessions, local+central submit revalidation, off-thread timeout/correlation dispatch, fixed Text Display policy/manager, compatibility and lifecycle hooks. Verification: three exact JVM tests plus one filtered GameTest passed; `:pet-fabric:remapJar` produced a 268,475-byte jar with 17 conversation/5 speech classes, both mixins, and zero testmod entries; handoff hash stayed `F94300D0DB4C6355431A076755C06CD1B99D0C3E5552313DC4D182F6F3C15A37`. Result: 13 tracker/acceptance items gained evidence with no AI on open/invalid access and one bounded current reply. Remaining: client visual yaw, nearby-player visibility, production dialogue HTTP/provider wiring, and deployed Villager-Interface coexistence.
- 2026-08-31 — AI operational resilience (Phase K): added strict `PET_AI_ENABLED` configuration, globally gated authority/dialogue/consolidation paths, synchronous executor-rejection containment, and per-component rollback guidance. Verification: exact config/dialogue/dispatcher tests passed 5/5 with zero failures/errors; static runtime review confirmed the kill switch does not gate adoption or physical mutation services. Result: 3 Phase K items gained evidence with minimal tests. Remaining: the switch is restart-scoped, production dialogue/provider wiring is still absent, and real outage/rollback staging remains open.
- 2026-08-31 — Authenticated metrics foundation (partial Phase K): added a fixed low-cardinality registry, authenticated Prometheus snapshot route, HTTP request/error/latency instrumentation, readiness health gauges, and deployment guidance. Verification: exact `PetHealthHttpHandlerTest` passed 1/1 and compilation wired all three runtime handlers through the metrics wrapper. Result: operational endpoint foundations are usable without exposing player identifiers or text, and the already-verified no-combat final criterion was audited closed. Remaining: feature-level metric producers and database-derived gauges listed on the Phase K item are intentionally still open.
- 2026-08-31 — Repeated physical lifecycle/ownership hardening (Sections 22.1, 22.2, 22.7): extended one existing GameTest through a second exact materialization, non-owner pickup denial, sleep continuity, and final owner pickup. Verification: the single filtered headless GameTest passed 1/1 with exact variant/scale/identity/revision assertions, zero unauthorized mutation, and four total authoritative revisions. Result: 3 acceptance items gained evidence without a broad suite. Remaining: process restart, backend transfer, concurrency, and deployed permission compatibility are separate open criteria.
- 2026-08-31 — Safe structured logging (Phase K): added the shared bounded JSON event builder and migrated all current service/Fabric log boundaries away from free-form values and throwable messages; dialogue events include hashed owner, pet/request correlation, model token counts, latency, outcome, and safe failure category. Verification: exact `StructuredPetEventTest` passed 1/1, service/Fabric compilation succeeded, and the production-source unsafe-log audit was empty. Result: 1 Phase K item gained evidence with one focused test. Remaining: select deployed log collection/retention and continue using the same boundary for future Velocity/Stripe/vector components.
- 2026-08-31 — Adaptive movement/obstacle verification (Section 22.3): expanded one existing headless follow scenario across far/medium/near bands and a timed solid obstruction/release while retaining teleport and forced-chunk guards. Verification: the single filtered GameTest passed 1/1 over 225 ticks; it closed far separation, visited all speed bands, stayed caged through the stuck timeout, and moved after release. Result: 3 movement acceptance items gained evidence without adding a second test launch. Remaining: wolf parity, varied/modded terrain, and performance/load staging.
- 2026-08-31 — Velocity whole-network presence (Phases E/H): added a separate non-routing Velocity plugin, strict timestamped wire contract, authenticated async transport with three retries, per-owner ordering, unique absence sessions, service ingestion, and persisted sleep integration. Verification: exact reporter and HTTP lifecycle tests passed 2/2; offline build produced a self-contained proxy JAR; subscriber audit found no backend-switch event. Result: 7 presence/sleep/relationship items gained evidence without changing WakeUpLobby admission or portal code. Remaining: deployed proxy-version/coexistence, private firewall reachability, live MariaDB observation, and alerting after retry exhaustion.

## Open Issues / Decisions Needed

- Actual deployed Fabric/Velocity versions are unverified; checked-in Loader/mapping/API versions and Velocity README/build metadata disagree.
- Automatic carry is currently guaranteed only for the signed ServerPortals path. Inventoried direct `/server`, `/connect`, `/forceserver`, `/return`, and third-party switches must be hooked into the same source preparation or explicitly routed/disabled before release.
- Transfer deployment must stage matching Pet Companion and MCServerPortals builds against live MariaDB/Velocity. In particular, kill/restart, destination outage, player disconnect, and the narrow destination-CAS-response-loss window need live observation; the persistent expiry/compensation paths are implemented but a lost successful completion response could require admin reconciliation until entity-missing recovery is added.
- Per owner clarification, Villager-Interface's captured/suppressed private chat is the intended input UI; the pet adaptation is implemented without its automatic greeting. A deployed two-mod test must still verify mixin ordering and the active-villager-session compatibility guard.
- `/pet adopt` now opens a clickable private menu and name-entry action; deployed client/coexistence verification remains useful, but the command/UI checklist item has local evidence.
- Admin recovery and current-period recall reset are implemented behind distinct level-3/provider permission nodes and backend-only transport; live MariaDB behavior and the deployed permission-provider/multi-backend operator drill remain unresolved.
- Velocity presence is implemented as the isolated `pet-velocity` plugin so it cannot mutate WakeUpLobby admission/portal state. Deployment must install the JAR, provide the shared token/private service origin, verify the actual proxy API version, and observe disconnect delivery plus service reachability before production rollout.
- Qdrant plus OpenAI `text-embedding-3-small` at 1536 dimensions are selected for the first hybrid vector path and both HTTP/configuration boundaries are implemented, but runtime worker scheduling, endpoint backup/sizing, and live isolation/load validation remain required before deployment.
- Dialogue core has no selected model/tokenizer/moderation provider or authenticated HTTP endpoint/client adapter. Fabric private input/session/display staging now exists behind `installConversationGateway`; production wiring must provide the central adapter while preserving authoritative context loading and exact schema/tokenizer.
- `PET_AI_ENABLED` is an environment/startup switch rather than a live admin toggle. This is intentionally fail-simple for the current runtime; deployed operations must restart the service after changing it and verify authenticated readiness plus physical authority routes.
- Consolidation core has no selected production model/tokenizer and is intentionally not wired into `PetServiceRuntime`; deployment wiring must install the sleep trigger/scheduler and embedding enqueuer after those provider/config decisions. Live MariaDB restart/rollback/lease tests remain unresolved; seven-day raw-text cleanup is now runtime-wired but also needs live observation.
- The permission boundary is provider-neutral; deployment must identify its permission-provider mod and install an adapter, or intentionally retain the documented vanilla permission-level defaults.
- Raspberry Pi MariaDB version/schema choice/credentials delivery/connection limits, secure service-host reachability, pet-service host, and Qdrant endpoint placement remain unverified deployment facts. Backup/firewall/secret tooling now exists, but the real host rules, existing backup integration, encrypted off-host retention, Qdrant backup/sizing, and disposable restore drill still require operator access.
- The historical website/Stripe repository, deployment/copy scripts, and a Minecraft 1.21.10 staging environment were not found in supplied workspaces. Per the approved no-typing clarification, pet-service now owns UUID-bound link generation, server-created hosted Checkout/Customer Portal, and signed webhook convergence without a website account. Actual test-mode/CLI delivery, Portal configuration in both Stripe environments, live V003 MariaDB behavior, TLS proxy validation, and production/test Stripe values remain unresolved deployment work.
- Runtime registry/entity evidence now covers exact scale values, marked/ordinary mechanics, compass data/targets, and server-side fixed Text Display yaw/follow/expiry; visual hitboxes/pathfinding, client-confirmed Text Display forward surface, client-rendered compass presentation, nearby visibility, and third-party/deployed backend-switch paths remain unverified.
- Section 22.9 has no numeric expected-concurrency, tick-time, write-rate, or pool-saturation thresholds; final quota and subscription price require measured telemetry/business decisions.
