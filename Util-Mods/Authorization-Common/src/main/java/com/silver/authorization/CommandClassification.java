package com.silver.authorization;

/** Central command-policy classifications shared by Velocity and Fabric. */
public enum CommandClassification {
    PUBLIC,
    PERMISSION,
    INTERNAL,
    UNMAPPED
}
